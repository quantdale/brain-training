/**
 * Theme constants — re-export of the canonical design tokens in `src/theme/tokens.ts`.
 *
 * Kept as a thin alias so existing components can keep importing from
 * `@/constants/theme`; new code may import directly from `@/theme/tokens`.
 */

export * from '@/theme/tokens';
