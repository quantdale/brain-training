/**
 * Per-game re-export of the shared `GameButton` (campaign 006R task 10.3
 * convention). Keep the local path stable for imports; mechanics stay in
 * per-game code.
 */
export { GameButton } from '@/components/game-ui';
export type { GameButtonProps } from '@/components/game-ui';
