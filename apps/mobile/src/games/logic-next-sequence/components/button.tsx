/**
 * Per-game re-export — migrated to shared `GameButton` (campaign 006R task 10.3).
 * Keep the local path stable for existing imports; mechanics stay shared.
 */
export { GameButton } from '@/components/game-ui';
export type { GameButtonProps } from '@/components/game-ui';
