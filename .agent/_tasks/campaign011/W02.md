# W02 — Validate + Harden: speed-order-sweep ("Order Sweep") + distinctness review

## Systems under test
`apps/mobile/src/games/speed-order-sweep/**` — Campaign 010 NEW game.

## Expected invariants
Unique token values per round (pairwise-distinct by construction); no trivially row-major-sorted boards; pause shifts ALL time anchors (deadline/round-start/last-clear) by one delta — speed gaps never include paused time; scoring `0.6·clearRatio + 0.4·meanSpeed`; value-addressed testIDs unique.

## Tests to write/run (adversarial)
- Generator property sweep: uniqueness, bounds, re-draw termination, per-round RNG fork independence.
- Timer races: window expiry exactly as last token clears; input after deadline ignored; rapid double-taps; tap on already-cleared token.
- Pause accounting: freeze mid-round → verify gaps/deadline shift; background/resume; repeated pause toggles.
- Scoring: empty session = 0; perfect instant clear = 1.0; wrong-tap-only sessions bounded; adaptive window band round-trip incl. malformed profile.
- QA force-win/lose validity; persistence shape vs analytics extractors; restart determinism.
- Screen test suite (`__tests__/scoring.test.ts`, `generator.test.ts`) — run for the first time; fix what they catch.

## DISTINCTNESS REVIEW (brief §9)
Compare mechanically against `speed-quick-compare` and `math-value-ordering` (both also ascending-order-ish). Evidence-based verdict in your packet: is Order Sweep's scan/search-among-many demand materially distinct? If weak, propose/implement a mechanic refinement (within the module) that restores distinction — do NOT delete the game unilaterally.

## Owned paths
The game directory above + `.agent/_tasks/campaign011/W02.md`. Read-only: quick-compare, value-ordering.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
