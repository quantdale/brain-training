# W08 — Personalization V2: signal correctness + ranking invariants

## Systems under test
`apps/mobile/src/personalization/**`, `src/workout/personalize.ts` (refactor surface), consumers.

## Known failing baseline
None in full run for personalization; the 43 pinned legacy `personalize.test.ts` tests passed at wave-1 convergence — re-run to confirm still green, then extend.

## Expected invariants
All 9 signals ∈ [0,1]; weights table documented and stable (`PERSONALIZATION_VERSION = 2`); stable tie-breaking; weak/stale strings word-identical to legacy; greedy composition penalty (−0.6 per same-category repeat) transparently itemized; determinism with injectable clock.

## Adversarial matrix (each = a test)
new user (no sessions/ratings) · one-session user · long history · highly skewed single-domain history · identical ratings everywhere · missing metrics fields · corrupt metric JSON rows · future timestamps · duplicate sessions same timestamp · category imbalance (8 domains, 1 game each vs all-in-one) · all games recently played (novelty=0 across board) · stale-boundary exactness (mirror db `isRatingStale` semantics) · rating exactly at INITIAL_RATING threshold.

## Explanation-text audit
Every emitted reason must correspond to its actual contribution (spot-check via property: zeroing a signal's weight removes/attenuates its reason). No impossible cycles in ranking when penalties apply.

## Tests to write/run
Property tests over randomized deterministic histories comparing against naive reference implementations of each signal; ranking monotonicity under controlled deltas; sanitize-path fuzz (garbage rows never throw).

## Owned paths
`src/personalization/**`, `src/workout/personalize.ts` + packet. today.ts integration → NEEDS_PARENT if signature-level changes needed.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
