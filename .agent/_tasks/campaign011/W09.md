# W09 — Analytics/Progress V2: metric equivalence vs reference implementations

## Systems under test
`apps/mobile/src/analytics/**` (new V2 modules + queries.ts), progress screens/charts (read-only; report defects).

## Known failing baseline (FIX FIRST — your own wave-1 suite)
`src/analytics/__tests__/analytics-v2.test.ts`: `buildSessionVolume` previous-window boundary — test expects exclusive edge ("old 40d ago outside"), implementation counts it. Triage: decide documented semantics (preceding equal-length window, half-open interval `[now-2w, now-w)` is the standard), fix IMPLEMENTATION to half-open, keep test, add boundary regression tests at exact edges.

## Mission
For every Campaign 010 metric build an authoritative naive reference in tests and prove equivalence across fixture classes: empty · 1 session · duplicate timestamps · same-day bursts · timezone edges (UTC vs local day boundary) · missing accuracy/reaction fields · forced sessions · mixed game versions · mixed difficulty versions · imported historical data · 20k-session scale (seeded generator).

## Coverage targets
trend-summary slope/consistency · volume-view windows · metric-trends availability gating · difficulty-progression neutrality · personal-best chains (strictly-improving, standing days) · rolling-windows trailing math · category-comparison vs DomainInsight reuse · workout-analytics completion runs · co-occurrence breadth (+ mandatory disclaimer presence) · training-balance evenness/weekly history · activity-calendar runs/weekday/monthly.

## No-fabrication audit
Every displayed number traces to stored rows; no efficacy language anywhere in new UI strings.

## Owned paths
`src/analytics/**`, `src/app/__tests__` progress render suites if needed, + packet. queries.ts edits allowed for correctness (keep public API).

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
