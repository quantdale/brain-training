# W15 — Platform / cross-platform / dependency audit

## Systems under test
`apps/mobile/package.json`, `app.json`, `metro.config.js`, `src/platform/**`, `screen-shell.tsx`, `theme/tokens.ts`, `sdk/audio-haptics-real.ts`, `db/adapters/**` (audit-only).

## Mission
Validate Campaign 010's cleanup actually holds and nothing broke.

## Checks
1. Removed-dependency hygiene: `@expo/ui, expo-glass-effect, expo-device, expo-image, expo-web-browser, expo-status-bar, expo-system-ui` — zero imports anywhere in src/jest/scripts/config; lockfile consistent (npm ls); metro resolves clean. Any straggler import = defect → fix or report.
2. Kept-deps verification: expo-linking (router), expo-document-picker + expo-sharing + expo-file-system (transport) — import surfaces compile for web too.
3. app.json plugin config: expo-audio flags (`microphonePermission:false, recordAudioAndroid:false, enableBackgroundPlayback:false`) correct per SDK 57 docs; no other plugin misconfig; scheme/icon/portrait intact.
4. AndroidManifest (untracked local): RECORD_AUDIO/FOREGROUND_SERVICE absence; data_extraction_rules XML validity (aapt-free static check); document the CNG reapply caveat precisely.
5. Platform paths: run web-targeted suites; grep for `Platform.OS` branches missing web/default; screen-shell inset math under mocked insets (0/34/50); tokens additive-only invariant (no renames vs 009 tag).
6. Haptics/audio fail-open: fireAndForgetHaptic rejection path unit test; audio-mode call unchanged per existing assertion.
7. Run: `npx expo-doctor` (record delta vs 009's 20/21), `npx expo export --platform web` smoke if cheap, dependency-graph check for accidental new transitive bloat.

## Explicitly out of scope
Broad upgrades; iOS build claims (remains NOT VALIDATED); gradle builds (device pass owns).

## Owned paths
Config/platform files listed above + packet. package.json edits only to FIX a proven defect (with NEEDS_PARENT note first unless trivially safe).

## Failure findings / Fixes performed / Remaining NOT VALIDATED

**Status: COMPLETED** (2026-08-21, worker W15, campaign011 wave 1)

### Check results (packet items 1–7)

1. **Removed-dependency hygiene — PASS.** Zero imports of all 7 removed packages
   (`@expo/ui`, `expo-glass-effect`, `expo-device`, `expo-image`, `expo-web-browser`,
   `expo-status-bar`, `expo-system-ui`) across `apps/mobile/src`, jest setup, scripts,
   and config — the only grep hits are docs/state/lockfile metadata. `npm ls` clean:
   `@expo/ui@57.0.11` + `expo-glass-effect@57.0.1` exist ONLY as hard dependencies of
   `expo-router@57.0.14` (verified in its lockfile dependency block); the other 5 are
   fully absent from `node_modules`. Lockfile consistent.
2. **Kept-deps verification — PASS.** `expo-file-system` / `expo-document-picker` /
   `expo-sharing` imported by `src/data-portability/file-transport.ts` (named imports
   `Directory, File, Paths` / `getDocumentAsync` / `isAvailableAsync, shareAsync`).
   `expo-linking`: no direct src import — router-facing only (hard dep of expo-router),
   matching the packet's "(router)" intent. Whole graph typechecks (`tsc --noEmit`
   clean on all data-portability + platform files). Web export smoke compiles the
   same graph end-to-end.
3. **app.json plugin config — PASS vs installed SDK 57 source** (`node_modules/expo-audio/plugin/src/withAudio.ts`,
   authoritative for the pinned version): `microphonePermission: false` is a valid
   `string | false` (suppresses NSMicrophoneUsageDescription); `recordAudioAndroid:
   false` overrides default **true** → RECORD_AUDIO never declared; `enableBackground
   Playback: false` overrides default **true** (load-bearing) → no FOREGROUND_SERVICE,
   no FOREGROUND_SERVICE_MEDIA_PLAYBACK, no AudioControlsService manifest entry, no iOS
   UIBackgroundModes 'audio'. `enableBackgroundRecording` unset = default false = desired.
   scheme `braintraining`, portrait orientation, and every referenced asset path exist
   on disk (incl. `assets/expo.icon` bundle). No other plugin misconfig found.
4. **AndroidManifest static audit — PASS.** Source manifest
   (`android/app/src/main/AndroidManifest.xml`, untracked by design —
   `apps/mobile/.gitignore:42`) declares only INTERNET + MODIFY_AUDIO_SETTINGS; grep
   across source + debug/debugOptimized + merged/packaged intermediates finds NO
   RECORD_AUDIO, NO FOREGROUND_SERVICE(_MEDIA_PLAYBACK); SYSTEM_ALERT_WINDOW appears
   only in debug-variant manifests (documented dev-overlay policy, absent from main).
   `data_extraction_rules.xml` + `backup_rules.xml` both well-formed (Python
   ElementTree parse) and consistent: DB-domain excluded from cloud-backup,
   device-transfer (API 31+) and full-backup-content (API ≤30). CNG caveat precise:
   these attributes + res/xml are hand-maintained because Expo config has no native
   keys; must be re-applied after any clean `expo prebuild`.
5. **Platform paths — PASS.** Branch-site sweep of ALL raw `Platform.OS/select` uses
   outside `src/platform`: screen-shell (web/tab-route/floored-pushed), pause-overlay
   (`!== 'android'` → imperative announce on iOS/web, live-region on Android),
   announcements (android binary, web gets imperative path), themed-text
   (`Platform.select({android}) ?? 500` fallback), tokens (explicit keys everywhere).
   No branch missing web/default handling. **Tokens additive-only invariant PASS**
   (diff vs `e0d92ce`, last campaign009 commit): BottomTabInset values unchanged
   (ios 50/android 80/web 64) with explicit `default: 0` added; additions only
   (`CompactLayoutMaxWidth`, `MediumLayoutMaxWidth`, `HomeIndicatorInset`); zero
   renames/removals in Colors/Fonts/Spacing/Radii/Typography.
6. **Haptics/audio fail-open — PINNED WITH NEW TESTS.**
   - NEW `src/sdk/__tests__/audio-haptics-fail-open.test.ts` (5 tests): installs a
     process `unhandledRejection` listener and proves `fireAndForgetHaptic`'s `.catch`
     swallows async rejections from `impactAsync`/`notificationAsync`; engine not
     poisoned after rejection (later haptics attempt backend, sfx still plays);
     `prepare()` resolves when `setAudioModeAsync` rejects AND still preloads all 7
     assets; `release()` clean after mid-session backend failure.
   - Audio-mode call shape unchanged: existing `audio-haptics.test.ts`
     "prepare configures audio mode" assertion untouched and green
     (`playsInSilentMode:true, interruptionMode:'mixWithOthers'`).
   - NEW `src/components/__tests__/screen-shell-inset.test.tsx` (6 tests): inset math
     under mocked SafeAreaInsetsContext — pushed routes honor insets 0→24 floor,
     34→34, 50→50; tab route ignores device inset (24); missing provider degrades to
     24 without throwing; web branch = exact token composition
     `BottomTabInset + Spacing.four`.

### Verification evidence

- `npx jest src/components/__tests__/screen-shell-inset.test.tsx src/sdk/__tests__/audio-haptics-fail-open.test.ts src/platform/__tests__/platform.test.ts src/sdk/__tests__/audio-haptics.test.ts --maxWorkers=2` → **4 suites, 31/31 PASS**.
- `npx tsc --noEmit` → 0 errors in all W15-owned files (2 new test files typecheck clean after fixing RNTL v14 API usage); see NEEDS_PARENT for 6 errors in other workers' in-flight suites.
- `npx expo-doctor` → **20/21**, exit 1: failing check is patch-version drift — expected `expo ~57.0.15` / `expo-router ~57.0.15` / `expo-linking ~57.0.7` vs pinned `.14/.14/.6`. Same score and same failing-check class as 009's recorded 20/21 ("pre-existing patch-version drift"); upstream published one new patch line since. Low severity; app green on current pins.
- Web export smoke (parent-sanctioned): `npx expo export --platform web --output-dir <temp>` → **exit 0, 20 static routes** emitted incl. `/game/[id]`, `(tabs)` group, `/storage-unavailable`; wa-sqlite wasm asset resolved via metro.config.js `assetExts.push('wasm')` (worker bundle 139KB emitted).
- Dependency-graph bloat check vs 009 lockfile (`e0d92ce`): 1135 → 1131 packages (net −4). Added: `expo-document-picker`, `expo-sharing` (intentional transport deps from commit fa14fea). Removed: `expo-device`, `expo-image`, `expo-status-bar`, `expo-system-ui`, `expo-web-browser` + transitive `ua-parser-js`. **No accidental bloat.**

### Defect classification

No Critical/High/Medium defects found in owned surfaces — Campaign 010's platform/deps cleanup HOLDS. Findings:

- **Low L1 — expo-doctor patch drift (pre-existing, tracked in KNOWN_ISSUES):** `expo`/`expo-router` @57.0.14, `expo-linking` @57.0.6 vs upstream ~57.0.15/~57.0.7. Symptom: doctor check fails. Root cause: upstream SDK 57 patch releases published after 010's pins. Fix: NOT taken — `npx expo install --check` during parent convergence; upgrading expo-router mid-campaign would churn every worker's surface. Evidence: doctor output above.
- **Low L2 — `expo-symbols` direct declaration redundant:** zero direct src imports; installed anyway as expo-router hard dep (npm ls shows dedupe). Hygiene-only removal candidate; NOT removed (package.json edit requires proven defect per packet; this isn't one). NEEDS_PARENT decision.
- **Low L3 — test-infra note (documentation, not code change):** RNTL v14 removed the UNSAFE_* accessors and its bundled `test-renderer` fork exposes only `root.queryAll(predicate)` (no `findByType`/`findByProps`); `render()` is async; `Platform.OS` is a plain data property requiring `jest.replaceProperty`. Future rendered-behavior tests should follow the patterns in `screen-shell-inset.test.tsx`.

### Fixes performed (all test-side; zero production edits needed)

- Added `src/components/__tests__/screen-shell-inset.test.tsx` (regression pin for audit B5 inset contract; includes justified expectation note: literal 64+24=88 only exists in a true web bundle — under jest-expo the module graph loads as ios so `BottomTabInset`=50; the pinned invariant is the exact token composition).
- Added `src/sdk/__tests__/audio-haptics-fail-open.test.ts` (regression pin: removing `.catch` from `fireAndForgetHaptic` turns these red deterministically via unhandledRejection listener).

### NEEDS_PARENT (shared-file decisions, no edits made by W15)

1. Converge expo patch drift (`npx expo install --check` → expo/expo-router/expo-linking ~57.0.15 line) or add `expo.install.exclude`; either way record in KNOWN_ISSUES.
2. Optional hygiene removal of direct `expo-symbols` declaration (zero direct usage; kept installed transitively by expo-router regardless).
3. Route to owners — 6 tsc errors currently present in NON-W15 in-flight suites (observed during whole-app typecheck, NOT touched by W15):
   - `src/analytics/__tests__/analytics-v2-equivalence.test.ts(684)` implicit-any index (W09)
   - `src/analytics/__tests__/projections-differential.test.ts(801)` string vs gameId union (W09)
   - `src/games/__tests__/catalog/non-migrated-qa-gates.test.ts(99)` possibly-undefined invoke (W06)
   - `src/games/__tests__/catalog/registry-membership.test.ts(78)` jest namespace as value (W06)
   - `src/games/__tests__/catalog/sensory-vocabulary.test.ts(177)` string vs HapticType (W06)
   - `src/games/memory-prospective-cue/__tests__/generator.adversarial.test.ts(106)` string vs difficulty union (W04)

### Remaining NOT VALIDATED (honest)

- iOS build/runtime (out of scope per packet).
- Android gradle build/device behavior (W16 owns emulator; manifest audit here is static-only, no aapt run).
- Full-suite Jest + lint (parent-owned; not run by W15 per concurrency policy).
- Real-DOM web rendering of ScreenShell (web branch verified via Platform.OS substitution + token composition in node env, not a browser DOM render).

