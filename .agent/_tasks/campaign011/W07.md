# W07 — Workout V2: full-lifecycle DB-backed validation

## Systems under test
`apps/mobile/src/workout/**`, `src/db/workout.ts`, workout UI (`components/workout/**`, home surfacing in `(tabs)/index.tsx` — read-only for UI; report UI defects).

## Mission
Prove the V2 state machine across every scenario class. Use REAL better-sqlite3 test DBs (repo pattern), not mocks, for integration coverage.

## Scenario matrix (each = a test)
new day · same-day relaunch · partial → resume → complete · completed reroll refusal · template change mid-day · length variants (short/standard/extended) · focus/domain templates · catalog growth between days · deleted/unknown game id reconciliation (invalid-id path) · completion advancing currentIndex exactly once · abandon · date rollover incl. timezone boundary + midnight + leap day (injectable clock) · process restart mid-workout · corrupt persisted instance JSON · zero eligible games · insufficient eligible games for a template · rotation determinism across a 7-day injected-clock sweep.

## Known failing baseline
None named in full run — but wave-1 worker admitted `workout/summary.ts` was finished post-truncation by the parent and its tests never ran. Run ALL workout suites first; expect surprises.

## Specific attack vectors
- Duplicate advance calls (double-tap next) → exactly-once.
- `listRecent(30)` ordering vs daily+template keys (`date` vs `date::template`) — W22 documented daily-before-template ordering; pin it.
- Completion summaries vs actual session records (position matching rule: most-recent-since-creation).
- Metadata versioning: old instances without metadata still load.

## Owned paths
`src/workout/**`, `src/db/workout.ts`, + packet. UI defects → report via NEEDS_PARENT (W24/parent surface).

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
