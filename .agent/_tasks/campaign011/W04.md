# W04 — Validate + Harden: memory-prospective-cue ("Cue Keeper")

## Systems under test
`apps/mobile/src/games/memory-prospective-cue/**` — Campaign 010 NEW game (prospective memory: brief signals, act on recurrence under delay; watchlist never re-displayed during play).

## Expected invariants
Each active signal appears exactly once per stream; retired glyphs can't rejoin as NEW same-round; ≥1 retirement when previous watchlist ≥2; round pass uses PER-ROUND tallies (the wave-1 bug fix — regression-test it); scoring table incl. false-alarm/miss penalties; normalization `signalAccuracy × (0.6 + 0.4 × accuracy)`.

## Tests to write/run (adversarial) — LIFECYCLE INTERRUPTION FOCUS
- Seeded schedule property sweep: uniqueness, near-duplicate guard, carry-over/retirement across rounds, adaptive ±1 step bounds.
- Interruption matrix: pause mid-stream item window; background between briefing and stream; quit mid-round → relaunch; tutorial open during stream; each must preserve exact remaining-window ms and tallies.
- False alarm on retired glyph vs filler timeout vs miss accounting; double-tap SIGNAL; tap GO on signal.
- Round-pass regression test pinning the per-round tally fix (session-cumulative bug class).
- QA force paths; persistence; adaptive profile decode validation; sensory aliases resolve.

## Known failing baseline
None for this module in the full run — tests never executed. Run them first.

## Owned paths
The game directory above + `.agent/_tasks/campaign011/W04.md`.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
