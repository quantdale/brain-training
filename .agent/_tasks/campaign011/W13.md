# W13 — Engagement V2: transactionality + idempotency attacks

## Systems under test
`src/achievements/**` (incl. chains.ts), `src/quests/**` (refresh/history), `src/rewards/**` (inbox/claim-all/history), `src/cosmetics/**` (collection), `src/streaks/**`, engagement db files (`db/achievements|quests|economy|ledger|xp-awards.ts`).

## Mission
Attack every reward path for double-spend / double-reward / loss. All on real DBs.

## Attack matrix (each = a test)
- claim single achievement twice → second is no-op, one ledger entry.
- claimAllRewards twice → idempotent; concurrent-ish interleaving (single + claim-all overlapping sets) → no duplication.
- same operationId replay (backup re-import scenario) → schema-level rejection tolerated gracefully.
- crash simulation: throw inside claim flow after ledger insert → verify transactional boundary (no partial state).
- already-owned cosmetic re-grant; insufficient balance purchase; duplicate streak-item application (009 invariant).
- quest refresh boundaries: daily at local midnight w/ DST-style offset clock injection; weekly ISO Monday edge; longterm never.
- chains: unknown stage id throws at resolve time (pinned); tier ordering monotonic; progress across mixed claim states.
- inbox aggregation correctness when achievements+quests+milestones overlap; restart persistence of claimedMilestones.
- provenance feed: merged XP+ledger ordering stable under equal timestamps.

## Invariants
No currency duplication. No reward loss. Every claim exactly-once per operationId. Ledger append-only preserved.

## Owned paths
The directories above + packet. db/index.ts additive needs → NEEDS_PARENT.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
