# W11 — Schema v9 migration matrix + repository correctness (BLOCKER-class owner)

## Systems under test
`apps/mobile/src/db/**` — schema.ts, migrate.ts, all repositories, adapters.

## Known failing baseline (FIX FIRST)
`src/db/__tests__/rating.test.ts`: `getHistory()` returns `ratingAfter: undefined` after a clamping apply (expected MIN_RATING). Suspected root cause: wave-1 refactor moved history SELECTs onto a shared column constant and dropped `rating_after` from the projection. Fix the SELECT; add a regression test asserting the full projected column set.

## Migration matrix (each = integration test on real better-sqlite3)
fresh v9 · v1→v9 · v2→v9 · … every intermediate → v9 · repeat initialization (idempotent) · v8-with-duplicate-legacy-rows → v9 (008's brick scenario) · corrupt user_version. Assert: no row loss (table counts before/after), append-only triggers recreated, indexes exist (`idx_rating_history_created_at`), foreign keys intact, backup schemaVersion tolerance.

## Repository correctness
- keyset pagination: no skips/dupes across page walks incl. equal sort keys; offset paging parity.
- `executeBatch`: all-or-nothing under mid-batch failure (rollback proof), nested-transaction behavior.
- aggregate pushdown vs JS reference on randomized fixtures.
- `listByIds` order preservation with missing ids; chunk boundary sizes.
- malformed historic JSON rows degrade to null without throwing (009 invariant).
- daily counts UTC vs local boundary parity with analytics/streak conventions.

## Owned paths
`src/db/**` + packet. Cross-file coordination (analytics consumers) via NEEDS_PARENT.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
