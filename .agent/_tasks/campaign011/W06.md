# W06 — Cross-catalog contracts over all 42 games

## Systems under test
Catalog-wide: `apps/mobile/src/games/**` (structural), `src/registry/**`, `src/sdk/__tests__/catalog-contracts.test.ts` (read-only; propose changes via NEEDS_PARENT).

## Mission
Extend and run parameterized contract validation across ALL 42 games using the generated registry. Verify every game has: metadata, definition, difficulty mapping incl. adaptive, screen, tutorial, session lifecycle, pause, QA hooks, testIDs, result model, versions, sensory vocabulary, persistence integration.

## Anti-pattern source audit (gameplay-critical code only)
Search all game modules for: `Date.now`, `Math.random`, uncontrolled `setInterval`/`setTimeout` (not via host helpers/injectable clock), hard-coded answers, unseeded generation, fabricated reaction times. Report each hit with context; fix true violations; document legitimate uses (e.g. clock injection seams).

## Known failing baseline
None directly — but you inherit verified-green status of `catalog-contracts.test.ts` from the parent's convergence run. Keep it green; extend coverage rather than weaken.

## Tests to write/run
- New parameterized suite over registry membership: per-game required-file + export-shape + versions monotonicity + adaptive-decode strictness.
- Cross-game uniqueness: module ids vs registry vs directory names; difficulty naming convention consistency check.
- Sensory vocabulary sweep beyond the existing scanner (e.g. haptic intensity bounds).
- Tutorial presence + QA-gate pattern for the 24 NOT-yet-migrated games too.

## Owned paths
New catalog-contract test files under `src/games/__tests__/catalog/**` or `src/sdk/__tests__/` (new file names only — do not edit existing suites except your own new ones) + packet. Repairs inside specific games → report to parent with exact diffs unless the game is unowned this wave.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
