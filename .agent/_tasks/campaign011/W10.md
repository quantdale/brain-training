# W10 — Query performance / JSON1 path: differential equivalence + measurement

## Systems under test
`src/analytics/projections.ts`, `src/analytics/queries.ts`, `src/db/sessions.ts` projection primitives, `scripts/perf/**`.

## Mission
Prove optimized paths return byte-equivalent business results vs the legacy full-row reference, then measure.

## Differential testing (the core deliverable)
Fixture generator: seeded randomized sessions (1k/5k/20k) across games/difficulties/versions with realistic rawResult JSON blobs + adversarial rows: malformed JSON (`json_valid=0`), NULL metrics, missing fields, old-version shapes, duplicate timestamps, same-ms completions.
For each fixture: run legacy full-row path AND each optimized path (repository primitive / transaction seam); assert identical extracted metrics per row and identical aggregates. Matrix: JSON1 available · forced-unavailable (probe override) · mixed valid/invalid rows.

## Boundary tests
Pagination keyset edges (limit+1 hasMore), ordering stability under equal keys, window boundaries inclusive/exclusive documented and pinned, `listProgressProjectionByGame` index usage (EXPLAIN QUERY PLAN assertions where supported).

## Measurement (only after equivalence)
Re-run `scripts/perf/` probe pattern at 1k/5k/20k for loadProgressSnapshot old-vs-new; record numbers in packet; compare to 009 baseline (101 ms @20k). Also measure backup export serialization improvement claim (~2.4 s @5k → ?). If any regression vs 009: fix before closing.

## Known failing baseline
None in full run (`projections.test.ts` passed convergence) — extend it.

## Owned paths
`analytics/projections.ts`, `analytics/queries.ts`, `db/sessions.ts`, `scripts/perf/**`, + packet.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
