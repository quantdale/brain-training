# W03 — Validate + Harden: math-value-ordering ("Value Order") + distinctness verdict

## Systems under test
`apps/mobile/src/games/math-value-ordering/**` — Campaign 010 NEW game.

## Expected invariants
Pairwise-distinct integer values (collision re-draws); correct order never ambiguous; per-round RNG forks; reducer clock-less (active-ms taps/ticks); budget freezes on pause; tiles' relative order never leaks via a11y; adaptive tile count [3→6] with linear challengeRating mapping.

## Tests to write/run (adversarial)
- Property sweep over seeds × difficulties: uniqueness, solvability, deterministic shuffle reproduction.
- Expression-disguised values (`6 × 4`): evaluation correctness, negative/intermediate handling if generator emits them.
- Boundary: tap sequence ascending-but-not-minimal (skips), duplicate taps, timeout at exact 0 ms, first-tap-wrong ends round.
- Pause mid-round freezes budget exactly; background/resume; finalizedRef double-submit guard.
- Scoring table incl. perfect-round time bonus bounds; adaptive round-trip; corrupt profile rejection.
- QA paths; persistence vs analytics extractors; tutorial idempotency; a11y labels.

## DISTINCTNESS VERDICT (brief §10)
Explicit comparison vs `speed-order-sweep`: does Value Order's arithmetic-evaluation-before-ranking demand survive playtest-level scrutiny? Document evidence. If redundant AND the math demand is not materially distinct, recommend (do not unilaterally execute) a redesign or removal to the parent.

## Known failing baseline
None for this module in the full-suite run — its test file has never been executed. Run it first.

## Owned paths
The game directory above + `.agent/_tasks/campaign011/W03.md`. Read-only: order-sweep.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
