# W05 — GameHost architecture: prove migrated games preserve legacy semantics

## Systems under test
`apps/mobile/src/components/game-host/**` + the 18 migrated games (batch 1: speed-reaction-time, math-fast-math, attention-visual-search, language-word-scramble, spatial-coordinate-turn, flexibility-card-sort; batch 2: attention-target-count, attention-symbol-tracker, memory-grid-recall, math-missing-operator, logic-next-sequence, spatial-fold-match, attention-odd-one-out, speed-quick-compare, memory-pattern-tap-back, flexibility-task-switch, logic-order-path, spatial-transform-match).

## Mission
Prove GameHost-migrated screens behave identically to their pre-migration semantics across the full lifecycle: mount → tutorial gating → start → pause → resume → back-guard → quit → force-win/lose → timeout → results → persistence → unmount → background → re-entry. Detect double-completion, orphan timers (fake-timer leak checks), duplicate result navigation, tutorial re-open, stale hook state, missing diagnostics metadata.

## Known failing baselines (fix within ownership)
- `src/games/math-fast-math/__tests__/screen.test.tsx`: version expectation 1.0.0 vs actual 1.1.0 — verify the REST of the suite passes semantically (score 720 path passed) then update version expectations with justification; investigate any NON-version failure as a real regression.
- `src/games/math-missing-operator/__tests__/game-definition.test.ts` + `screen.test.tsx`: same class (1.1.0 bump from content wave) + any deeper breakage.
- W18's declared risk areas: odd-one-out tick cadence after useGameInterval conversion (interval no longer restarts on deadlineMs change); quick-compare/order-path 0 ms-remaining expiry edge now fires on next tick instead of synchronously. Write regression tests pinning correct behavior; fix production if semantics actually changed.

## Tests to write/run
- Shared host unit tests: session-identity collision safety, claimFinalize exactly-once ordering vs guard failures, canPause phase gating, back-handler subscribe/dispose, interceptBack only while inSession.
- Per-migrated-game screen suites: run ALL existing ones; fix failures at root.
- Differential spot-checks vs one legacy (unmigrated) game for lifecycle parity.

## Owned paths
`components/game-host/**`, the 18 migrated game directories' screen/test files, + packet. NOT the contract scanner (parent) — propose changes via NEEDS_PARENT.

## Failure findings / Fixes performed / Remaining NOT VALIDATED
(filled)
