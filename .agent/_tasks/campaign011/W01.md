# W01 — Validate + Harden: attention-sustained-vigilance ("Signal Watch")

## Systems under test
`apps/mobile/src/games/attention-sustained-vigilance/**` — Campaign 010 NEW game, never executed against a test suite until now.

## Expected invariants
Deterministic `(seed, difficulty) → stream`; stop-digit exclusivity (`isTarget ⇔ digit === stopDigit`); exact `expectedTargetCount`; `minTargetGap` respected; RT from monotonic lifecycle deltas only (no wall clock); pause freezes windows exactly; commission penalty floors score at 0; normalizer perfect run = 1.0; challenge hidden from a11y while paused; lifecycle finalization exactly once.

## Tests to write/run (adversarial)
- Property sweep over seeds × difficulties: stream invariants + replay determinism.
- Commission/omission accounting incl. double-tap on GO, tap during feedback/ISI, tap on stop digit, timeout vs explicit withhold.
- Window timeout at exact boundary (0 ms remaining); drift-free scheduled onsets across 100+ trials.
- Pause/resume mid-window, mid-ISI, during feedback; AppState background injection; resume does not re-open finished sessions.
- QA force-win/force-lose paths produce valid persisted results; adaptive profile round-trip incl. corrupt/malformed stored profile JSON.
- Scoring monotonicity (more hits ≥ same or better value at fixed speed); accessibility tree never leaks the stop digit while paused; tutorial completion/skip idempotency.
- Persisted result shape matches `dbSessionPersister` expectations and analytics extractors (score/accuracy/reaction fields present).

## Known failing baseline
None named by the full suite for this module — its own 2 test files were NEVER RUN before this campaign. Run them first.

## Owned paths
The game directory above + `.agent/_tasks/campaign011/W01.md`.

## Failure findings
(filled)

## Fixes performed
(filled)

## Remaining NOT VALIDATED
(filled)
