# W02 — GameHost Migration Batch B (medium multi-round)

**Status:** PENDING

## Mission
Migrate 6 medium-complexity legacy games onto `@/components/game-host`:
`language-context-fit`, `language-word-chain`, `logic-code-cracker`, `math-value-ordering`,
`speed-order-sweep`, `flexibility-cue-shift`.

Same contract, method, and exemplars as W01 (read that packet).

## Owned paths (write)
- `apps/mobile/src/games/language-context-fit/**`
- `apps/mobile/src/games/language-word-chain/**`
- `apps/mobile/src/games/logic-code-cracker/**`
- `apps/mobile/src/games/math-value-ordering/**`
- `apps/mobile/src/games/speed-order-sweep/**`
- `apps/mobile/src/games/flexibility-cue-shift/**`

## Read-only dependencies
`src/components/game-host/**`, `src/components/game-ui/**`, `src/sdk/**`;
exemplars `.agent/_tasks/campaign010/W17.md`/`W18.md` + `games/math-fast-math/screen.tsx`.

## Forbidden paths
Everything else (other games, game-host itself, scanner, registry, manifests, scripts, docs).

## Implementation targets
W01 targets apply. Multi-round/multi-phase care:
- round advancement must keep using lifecycle active-only elapsed ms for any pacing
- pause state per phase: extend `canPause()` to exactly the pausable phases
- results transitions unchanged; QA force-win must still terminate every phase machine
- `flexibility-cue-shift`: keep the 009 SFX alias fixes; `logic-code-cracker`: keep solver
  validation untouched

## Validation expectations
`tsc --noEmit` clean; owned suites + catalog-contracts suite green.

## Shared changes requested from parent
_(worker fills)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
