# W05 — GameHost Architecture Consolidation

**Status:** COMPLETED

## Mission
Own and mature `apps/mobile/src/components/game-host/**` so the four migration batches
(W01–W04, running in parallel) can land cleanly. Identify repeated compatibility hacks
across the 18 already-migrated games and improve the abstraction ONLY where it reduces
real complexity.

## Owned paths (write)
- `apps/mobile/src/components/game-host/**` (including its `__tests__`)

## Read-only dependencies
- All game modules (read-only; find repeated patterns)
- `src/components/game-ui/**`, `src/sdk/**` (consume only)
- Campaign 010 W17/W18 NEEDS_PARENT notes (e.g. W18's `qaPanelPosition` observation)

## Forbidden paths
All game modules (report needed per-game follow-ups instead), contract scanner
(`src/sdk/__tests__/catalog-contracts.test.ts` — do NOT edit), registry, manifests,
scripts, docs.

## Implementation targets (candidate list — evidence-gated)
- round-aware helpers only if ≥3 games would share real logic
- result routing / session completion guard hardening
- pause decoration + QA panel positioning cleanup
- timer adapters (`useGameInterval`/`useGameTimeout`) review for drift after resume
- background-resume hooks: single AppState subscription story
- GameHost error boundary: crash inside a game view must surface the host error state
- DO NOT add APIs to satisfy one odd game when local composition is cleaner
- MUST remain backward-compatible with the current API while batches run:
  additive props/helpers only; no signature breaks without a NEEDS_PARENT flag

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: `src/components/game-host/__tests__/**` +
  `src/sdk/__tests__/catalog-contracts.test.ts` green
- Spot-check at least 3 migrated games' screen suites still pass if you touch shared paths

## Shared changes requested from parent
_(worker fills)_

## Completion summary

Host consolidation (additive-only): GameSessionController.resumeIfPaused() guard added (crash-safe double-tap Resume; removes lifecycle-status reads from screens); misleading resume() doc corrected. Evidence-negative skips documented: route-level ErrorBoundary already covers game-view crashes; qaPanelPosition/timers/AppState reviewed sound. Follow-up executed via dedicated wave: ALL 42 screens adopted resumeIfPaused(); 2 local status guards deleted (attention-sustained-vigilance, math-value-ordering); grep-verified 0 unguarded session.resume() calls remain. Validation: host+contract 46/46; spot-checks 290/290; full games sweep after adoption 310 suites/3764 tests PASS.

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
