# W16 — iOS Static Preparation

**Status:** DONE

## Mission

Improve iOS source-level compatibility where safe. No iOS build/runtime validation exists
on this host — final status stays `NOT VALIDATED — requires macOS/Xcode`. Do not pretend
otherwise.

## Owned paths (write)

- Platform-seam source files: `src/hooks/use-color-scheme*`, platform adapters,
  safe-area/keyboard seams, `src/sdk/**` platform branches — behavior-preserving only
- A static audit report appended to your Completion summary

## Read-only dependencies

- All screens/games (read-only), `app.json` ios section, prior audits
  (`docs/audits/campaign009-xplat-audit.md`)

## Forbidden paths

ios/ generated dirs (never commit), android config (W14), games' mechanics, workout,
scripts/qa harness, manifests. Any change needing a native module → forbidden; reported instead.

## Implementation targets

1. Refresh the static audit: Platform.OS branches, file APIs (expo-file-system paths),
   sharing/picker seams, permissions strings, safe areas, keyboard handling,
   audio/haptics session behavior, navigation (NativeTabs iOS capability), storage paths
2. Implement safe source-level fixes found with tests where practical
3. Confirm app.json ios section validity (bundle id, icon, entitlements seams)
4. Record every finding: fixed / seam-adequate / needs-native (deferred)

## Shared changes requested from parent

1. **Adopt `KeyboardAvoidingArea` on the two TextInput screens** (`data-management.tsx`
   import/export fields, `(tabs)/games.tsx` search field — sibling-owned, read-only this
   packet). The seam now supports a `keyboardVerticalOffset` prop (header compensation);
   on iOS it mounts a real `KeyboardAvoidingView` (`padding`), on Android/web it is an
   intentional passthrough. Without adoption, long text-entry content can sit under the
   keyboard on iPhones (adjustResize does not exist there).
2. **`ios.supportsTablet` is unset** in `app.json` (Expo defaults true) while orientation
   is portrait-locked. Set it explicitly (true + iPad layout QA, or false) before any
   TestFlight submission. Carry-over of campaign009 C3.
3. **Verify at first macOS prebuild** that the expo-audio plugin
   (`microphonePermission: false`) suppresses `NSMicrophoneUsageDescription` in the
   generated Info.plist. Carry-over of C4; nothing more can be done from source here.
4. **Status bar styling (C5 carry-over):** no explicit `expo-status-bar` wiring exists;
   dark screens (e.g. storage-unavailable `#0b0d12`) rely on system defaults. Parent may
   want explicit StatusBar control in `_layout.tsx` after device contrast checks.
5. **NativeTabs testID contract re-verification on iOS hardware** after every SDK bump
   (B3 carry-over; `unstable-native-tabs` API).

## Completion summary

### Scope & method

Refreshed the campaign009 xplat audit against the current tree (`main` @ 0669f8e +
concurrent sibling work). Full greps over `apps/mobile/src` for `Platform.OS/select`,
`Dimensions.get`, `useWindowDimensions`, `BackHandler`, `KeyboardAvoidingView`,
`Keyboard.`, `LayoutAnimation`, `UIManager`, `AppState`, shadow/elevation,
`PanResponder`, DOM globals (`window.`/`document.`/`navigator.`), `toLocale*String`,
`NativeModules`/`requireNativeModule`, `maxFontSizeMultiplier`/`allowFontScaling`,
file-API/path patterns (`file://`, `documentDirectory`, `.sqlite`), sharing/picker usage;
deep reads of `src/platform/**`, `src/sdk/**`, hooks, theme tokens, screen-shell,
app-tabs (+web), tabs constants, a11y modules, data-portability transports, jest setup,
`app.json`, committed Android manifest (`windowSoftInputMode=adjustResize` confirmed),
and RN 0.86.2 `KeyboardAvoidingView.js` source (for behavior-preservation proof below).
Concurrent-edit caveat: siblings were actively editing `game-detail/[id].tsx`,
workout/**, game-host/**, scripts/perf during this packet; one transient tsc error set in
`game-detail/[id].tsx` appeared and vanished between my runs. Findings below are scoped
to my read-only observations of those files.

### Audit findings table

| # | Area | Finding | Classification |
|---|------|---------|----------------|
| F1 | `src/platform/keyboard.tsx` | On Android/web the seam mounted RN `KeyboardAvoidingView` with `behavior: undefined`. Verified against RN 0.86.2 source: undefined-behavior renders the **default branch = plain `<View>`** (output-identical) but still subscribes 2 keyboard listeners + layout bookkeeping that can never affect output. Also: no way to pass `keyboardVerticalOffset` through the component even though the config function supported it (zero consumers today, so purely additive). | **fixed-now** |
| F2 | `src/sdk/audio-haptics-real.ts:21` | Dead `import { Platform } from 'react-native'` — referenced only by a comment (line 192); the code intentionally calls `setAudioModeAsync` unconditionally with fail-open catch. Removed. | **fixed-now** |
| F3 | `src/platform/__tests__/keyboard-area.test.tsx` | New suite (4 tests) pinning the mount-path contract: iOS mounts an avoidance view (Keyboard listener signal); Android/web mount none (guards double-compensation); offset forwarding mapped verbatim. Traversal-based detection proved unusable (jest-expo node env collapses composite containers out of the RNTL tree — only leaf hosts visible), so detection spies `Keyboard.addListener`, which the real KAV calls in `componentDidMount` and a plain View never does. | **fixed-now** |
| S1 | `src/platform/capabilities.ts` | Single-source OS branching (`platformKind` narrowed to ios/android/web); haptics capability queries fail-open; web-only DOM probes short-circuited behind platform check. | **seam-adequate** |
| S2 | `src/platform/layout.ts` | Reactive viewport reads exclusively via `useWindowDimensions`; shared breakpoints from tokens. No static `Dimensions.get` anywhere in src (re-verified zero hits). | **seam-adequate** |
| S3 | `src/platform/touch.ts` | 44pt minimum target via hit-slop math, null when already compliant; covered by unit tests. | **seam-adequate** |
| S4 | `src/hooks/use-color-scheme{,.web}.ts` | Native re-exports RN hook; web variant returns "light" until hydration (standard Expo template pattern). No iOS gap. | **seam-adequate** |
| S5 | Safe areas | `screen-shell.tsx` `resolveBottomPadding`: web = floating-bar reserve + spacing; native tab routes = spacing only (tab host absorbs inset); pushed routes = max(real bottom inset, 24) — campaign009 B5 fix present and pinned by `screen-shell-inset.test.tsx` (matrix 0/34/50 + missing-provider fallback). `BottomTabInset` now carries an explicit `web: 64` key (A2 fix present). `HomeIndicatorInset` token is documentation/QA reference only. | **seam-adequate** |
| S6 | A11y announcements | `LiveRegion`: Android gets a real live-region node; iOS/web get imperative `announceForAccessibility` only (avoids double announcements). Whitespace trim-guard. Correct platform split for how engines actually behave. | **seam-adequate** |
| S7 | Reduced motion | Shared `usePrefersReducedMotion` + pure `motionValue`/`reduceDuration`; consumed by celebration animation. Works identically via AccessibilityInfo on both platforms. | **seam-adequate** |
| S8 | Font scaling | `ThemedText` caps `MAX_FONT_SCALE` by default, allows explicit override or `allowFontScaling={false}` opt-out for board glyphs (B1 fix present; games use glyph opt-outs; GameButton min-height 44). Covers iOS Dynamic Type. | **seam-adequate** |
| S9 | Shadows/elevation | Celebration card sets iOS shadow props AND `elevation: 4` (A3 fix present). Only shadow site in src. | **seam-adequate** |
| S10 | File APIs / storage paths | `file-transport.ts`: lazy `require` of expo-file-system/document-picker/sharing (stale-binary crash fix retained); backup dir derived from `fs.Paths.document` — **zero hardcoded paths** in src; DB opened by name via `openDatabaseSync`. `pickBackupFile` uses `copyToCacheDirectory: true` so Android `content://` URIs become readable files and iOS cache copies work — correct on both platforms. | **seam-adequate** |
| S11 | Sharing seam | `shareBackupFile` guards `isAvailableAsync()` and resolves false when unavailable; JSON mime + title. Works with iOS share sheet and Android sheet unchanged. | **seam-adequate** |
| S12 | Permissions strings | `app.json` expo-audio plugin: `microphonePermission: false`, `recordAudioAndroid: false` — mic string suppressed at plugin level (C4 verification remains macOS-only). Nothing in src requests permissions directly (re-verified: no `Permissions.` / `request.*Async` outside mocks). | **seam-adequate** |
| S13 | Audio/haptics session | `setAudioModeAsync({ playsInSilentMode: true, interruptionMode: 'mixWithOthers' })` — deliberate iOS mute-switch override + mixing, documented; every native call fire-and-forget with attached `.catch` (fail-open on missing backend incl. web). | **seam-adequate** |
| S14 | Navigation / NativeTabs | Native tabs render from shared `TAB_DEFINITIONS` with per-platform icon triple (`sf`/`md`/`web`); web bar isolated via `app-tabs.web.tsx` extension discipline; `expo-symbols` imported type-only in constants. Residual: unstable API + per-platform rendering differences (see D3). | **seam-adequate** |
| S15 | Back handling | `BackHandler` now used by `game-host.tsx` (session-aware) and `a11y/dialog.tsx` — campaign009 B6 ("back silently discards session") addressed since the audit. Sibling-owned; verified present, not modified. | **seam-adequate** |
| S16 | Locale-dependent dates | `toLocaleDateString()` UI-only usages remain (results/progress/game-detail/shell format) — informational per B8; deterministic UTC formatting lives in `analytics/format.ts` where tests need it. Unchanged (correct split). | **seam-adequate** |

### app.json ios section validity (report-only; manifest is a forbidden surface)

- `bundleIdentifier: "com.braintraining.app"` — valid reverse-DNS; matches Android package.
- `icon: "./assets/expo.icon"` — exists as the SDK 57 liquid-glass **directory** format
  (`assets/expo.icon/icon.json` + `Assets/grid.png`, `Assets/expo-symbol 2.svg`). Valid
  for Expo ~57; actual liquid-glass processing only runs at prebuild on macOS.
- Orientation portrait lock, `userInterfaceStyle: automatic`, scheme `braintraining` — valid.
- No `ios.entitlements` keys — none required today (no push/iCloud/keychain/associative domains in product code).
- No `ios.infoPlist` overrides — plugin-derived strings only; expo-audio mic string disabled at plugin level (verify at prebuild, item 3 above).
- Gap: `ios.supportsTablet` unset (Expo default true) — see parent item 2.

### Validation (from apps/mobile)

- `npx tsc --noEmit`: **clean for every file I touched**. Residual errors are in
  sibling-owned, untracked `src/__tests__/perf-quest-eval-ab.test.ts` (3 errors, present
  since my baseline run, untouched by me). A second transient set in sibling-owned
  `game-detail/[id].tsx` appeared and disappeared between runs (live concurrent edit).
- Jest targeted: **5 suites / 35 tests / 35 passed / 0 failed / 0 skipped**
  (`src/platform/__tests__/platform.test.ts`, new `keyboard-area.test.tsx` (4),
  `sdk/__tests__/audio-haptics.test.ts`, `audio-haptics-fail-open.test.ts`,
  `components/__tests__/screen-shell-inset.test.tsx` as untouched-seam sanity).
- ESLint (eslint-config-expo) clean on all three touched files.

### Files changed + why behavior-preserving

1. `apps/mobile/src/platform/keyboard.tsx` — Android/web branch renders a plain `View`
   instead of a `behavior: undefined` `KeyboardAvoidingView`; iOS branch unchanged
   (`padding` + forwarded offset). Proof: RN 0.86.2 `KeyboardAvoidingView.js` render()
   destructures `behavior`/`keyboardVerticalOffset` out of props and its `default:`
   switch branch returns `<View style>{children}</View>` regardless of keyboard state —
   so output was and remains a plain View; only dead listener subscriptions are gone
   (Android's manifest-pinned `adjustResize` owns insets). Added optional
   `keyboardVerticalOffset` prop (additive; zero existing consumers — grep-verified).
2. `apps/mobile/src/sdk/audio-haptics-real.ts` — removed one unused import line; no
   executable change.
3. `apps/mobile/src/platform/__tests__/keyboard-area.test.tsx` — new tests only.

### Honest validation statement

**iOS build/runtime = NOT VALIDATED — requires macOS/Xcode.** Everything above is
static/source-level analysis plus Node-side jest coverage under jest-expo. No iOS
prebuild, pod install, simulator, or device run happened or could happen on this host.
The final iOS status remains NOT VALIDATED and this report claims no otherwise.

## Remaining debt

1. **D1 — Keyboard avoidance adoption** (parent item 1): seam exists + tested, but no
   screen uses it yet; iPhone text-entry UX under keyboard unverified on-device.
2. **D2 — iPad posture**: `supportsTablet` decision + iPad layout pass (portrait-locked,
   `MaxContentWidth` centering helps but nothing device-tested).
3. **D3 — NativeTabs instability**: `expo-router/unstable-native-tabs`; per-platform
   rendering (UITabBar/Material 3) and best-effort `testID` mapping must be re-verified
   after every SDK upgrade on both platforms.
4. **D4 — macOS-gated checklist** (C1/C2/C4/C5/C6 carry-over): first prebuild+pod+Xcode
   build; new-arch behaviors (celebration core-Animated, NativeTabs host, expo-audio
   players) on simulator; Info.plist mic-string absence; status-bar contrast in light
   mode on dark screens; pushed-route bottom inset on Dynamic Island/home-indicator
   hardware (source-level fix is in and unit-pinned; device confirmation outstanding).
5. **D5 — Web font-scale edge**: `useColorScheme` web hydration flash (light→system) is
   template-standard; left untouched deliberately (behavior-preserving mandate).
