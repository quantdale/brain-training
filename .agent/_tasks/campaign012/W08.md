# W08 — Workout Device / Harness Support

**Status:** COMPLETED

## Mission
Extend the QA harness so the parent can autonomously traverse Workout V2 template flows
on the emulator: short, standard, focus/domain template, resume/relaunch.

## Owned paths (write)
- `scripts/qa/**` (autobot.mjs + helpers + README)

## Read-only dependencies
- Home workout testIDs as implemented by W07 (read current committed state; if W07 adds
  new testIDs after your pass, list expected IDs in your packet and note the dependency)
- Campaign 011 run artifacts (`qa-artifacts/20260822-113955-autobot-workout`) for the
  proven default-journey flow

## Forbidden paths
App source (`apps/mobile/src/**`) — if traversal requires a product change or new testID,
record it as NEEDS_PARENT/product-request instead. No manifests/registry/docs edits.

## Implementation targets
- New autobot mode(s)/flows: `workout-short`, `workout-focus` (and parameterized length)
- Template row → start → play-through → advance chain reusing existing drive helpers
- Resume/relaunch probe: kill app mid-workout, relaunch, verify resumable state persists
- Machine-readable failure artifacts consistent with existing modes
- One exclusive device owner rule documented: harness never runs two drivers

Do NOT launch long device journeys yourself — the parent owns exclusive emulator sessions.
You may smoke-check script syntax/logic offline (node --check / dry-run listing).

## Validation expectations
- `node --check scripts/qa/autobot.mjs` clean
- Offline dry-run of flow definitions (no device) where the harness supports it
- README documents the new flows + prerequisites

## Shared changes requested from parent
_(worker fills)_

## Completion summary

QA harness: new autobot modes workout-short, workout-focus (requireFocus guard), workout-resume (mandatory mid-workout kill/relaunch probe); parameterized --length/--template validated OFFLINE pre-device; --list-flows machine-readable flow listing; structured failure artifacts + screenshots at selection/pre-kill/resume/completion; exclusive-device-owner rule documented. Consumes W07 testID inventory (verified present on main). Validation: node --check clean; --self-test 28/28 PASS incl. 11 new assertions; BLOCKED path yields structured artifacts. On-device journeys are PARENT-OWNED (see VALIDATION.md).

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
