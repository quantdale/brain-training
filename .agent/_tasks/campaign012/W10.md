# W10 — Global Content Integrity (non-math catalog)

**Status:** COMPLETED

## Mission
Audit the 38 non-math games' content/generators for dead or low-quality content and fix
via metadata/content changes (NOT mechanic rewrites):
- unreachable content / impossible difficulty branches
- duplicate prompt banks / near-duplicate items
- dead variants, low-entropy generators
- content collisions, ambiguous answers

## Owned paths (write)
- Non-math game `generator.ts`, content/pack files, difficulty banks, and their
  generator/difficulty tests — for: memory*, attention*, speed*, language*, logic*,
  flexibility*, spatial* modules' CONTENT files only
- Exception: do not edit any file inside a module while a migration batch owns that
  module's screen/components — content files are disjoint from screen/components/tests,
  so coordinate only if a true overlap appears (report it)

## Read-only dependencies
- All screens/reducers/scoring (read-only)
- W09 owns ALL math-* content — exclude math from your fixes; note math findings in your report

## Forbidden paths
math-* content (W09), all screens/components/session/types files, registry, manifests,
scripts, docs.

## Implementation targets
1. Systematic audit pass per category (scripted where possible: enumerate templates/items,
   simulate generation across seeds/difficulties, flag never-generated branches)
2. Fix findings via content/metadata edits; bump contentVersion where item semantics change
3. Ambiguous answers: verify unique-solution invariants where the mechanic promises them
4. Document audit method + findings table in your Completion summary

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: touched content suites green

## Shared changes requested from parent
_(worker fills)_

## Completion summary

Global non-math content integrity audit: permanent deterministic tripwire sweep (catalog-integrity-sweep.test.ts) simulating FULL session generation for all 37 non-math games x 4 difficulties x 8 seeds (~10k rounds): determinism, salt sensitivity, entropy, option/distractor invariants, branch coverage (11 branch-heavy games incl. recipe families/rules/folds/transforms/congruency), plus 17 exported validators and static bank analysis. RESULT: catalog CLEAN - 0 findings requiring fixes (campaigns 009/011 fixes holding); word-chain expert pool 9-vs-8 documented Low debt (usedChainIds prevents repeats). No version bumps warranted (no item semantics changed). Note: repo has 37 non-math + 5 math = 42 total (packet said 38).

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
