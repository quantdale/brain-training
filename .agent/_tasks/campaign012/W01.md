# W01 — GameHost Migration Batch A (simple timer/response)

**Status:** PENDING

## Mission
Migrate 6 simple timer/response legacy games onto `@/components/game-host`:
`speed-tap-rush`, `speed-color-match`, `attention-sustained-vigilance`, `memory-sequence-memory`,
`flexibility-rule-flip`, `memory-running-order`.

## Owned paths (write)
- `apps/mobile/src/games/speed-tap-rush/**`
- `apps/mobile/src/games/speed-color-match/**`
- `apps/mobile/src/games/attention-sustained-vigilance/**`
- `apps/mobile/src/games/memory-sequence-memory/**`
- `apps/mobile/src/games/flexibility-rule-flip/**`
- `apps/mobile/src/games/memory-running-order/**`

## Read-only dependencies
- `src/components/game-host/**` (consume; never edit — NEEDS_PARENT for API gaps)
- `src/components/game-ui/**`, `src/sdk/**` (consume only)
- Campaign 010 exemplars: `.agent/_tasks/campaign010/W17.md`, `W18.md`; `games/math-fast-math/screen.tsx`

## Forbidden paths
Everything else: other games, game-host itself, contract scanner, registry, manifests,
app config, scripts, docs, `.agent/`.

## Implementation targets
- Replace per-screen duplicated session id/seed creation, direct `SessionLifecycle` ownership,
  `finalizedRef`, AppState auto-pause effect, intro/difficulty/start layout, in-session header,
  QA-panel gating, pause-overlay mount, results chrome with `<GameHost>` + `useGameSession` +
  `<GameResults>` (+ `useGameInterval`/`useGameTimeout` where pacing timers exist).
- Seed flows through `resolveSessionSeed(...)`; finalization via `claimFinalize()` /
  `completeIfActive()` / `elapsedMs()` / `pausedDurationMs()`.
- Delete now-dead per-game `components/pause-overlay.tsx` re-exports.
- Hardware-back guard: `interceptBack={inSession}`.

## Preservation contract
Do NOT change: mechanics, reducer/generator/scoring/session logic, testIDs, tutorial behavior,
QA force states, a11y labels/roles, sensory events (`liveAudioHaptics` calls), persistence shape,
difficulty semantics. Timing must stay monotonic-clock-based; paused time excluded.

Special care:
- `speed-color-match`: keep the 009 monotonic-timing fixes intact (gameVersion history).
- `memory-sequence-memory`: keep force-perfect QA extra (QaPanelShell `extraActions` or host slot).
- Grid/decorative boards that unmount while paused (011 a11y fix) must keep doing so if present.

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: owned modules' suites + `src/sdk/__tests__/catalog-contracts.test.ts` green

## Shared changes requested from parent
Record NEEDS_PARENT items here (GameHost API gaps, shared-file needs).

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
