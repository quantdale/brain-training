# W15 — Dependency Audit

**Status:** COMPLETE (2026-08-23)

## Mission
Controlled audit of the dependency surface after Campaign 011's intentional patch pins.
Report-first: the parent applies any lockfile/manifest change.

## Owned paths (write)
- `.agent/_tasks/campaign012/W15.md` (your findings) — that is all you may write
- Optionally a throwaway audit script under `C:\Users\palac\AppData\Local\Temp\opencode`

## Read-only dependencies
- `apps/mobile/package.json`, root `package.json`, lockfiles, `app.json` install.exclude,
  `node_modules` metadata (`npm ls <pkg>`), `npx expo-doctor` output

## Forbidden paths
package.json / package-lock.json / app.json edits (parent applies approved changes),
all source, scripts, docs.

## Implementation targets
1. Inventory current pins vs SDK-expected versions: expo, expo-router, expo-linking,
   native modules (sqlite, audio, haptics, asset, file-system, sharing, etc.), react-native,
   test tooling
2. Classify each drift: safe patch bump / unsafe churn / deprecated / unused /
   transitive-required
3. Explicitly evaluate lifting the 011 `expo.install.exclude` pins (expo ~57.0.14-era):
   what changed upstream? risk of bumping together as a group?
4. Unused-dependency check (imports vs declared)
5. Produce a concrete recommendation table: package | current | proposed | action | risk |
   evidence. NO changes without parent approval.

## Validation expectations
- Commands run read-only; record outputs in your packet
- No working-tree modifications outside your packet file

## Shared changes requested from parent
See "Recommendation table" + "Exact manifest edit requested" below.

## Completion summary
DONE 2026-08-23 — see appended audit report below. All commands read-only; only this file written.

## Remaining debt
Changelogs for trio patch releases not fetchable programmatically (atom feed lacks entries; GitHub API tag guesses 404) — risk assessed via tarball dependency-metadata diff instead. Tooling majors (ts7/eslint10/jest30) deferred, tracked below.

---

# W15 AUDIT REPORT (2026-08-23)

## 0. Brief corrections

- The 011 pins live in `apps/mobile/package.json` → `expo.install.exclude: ["expo","expo-linking","expo-router"]` (commit `6d04318`, 2026-08-22), **not** in `app.json`. `app.json` has no install/sdkVersion keys.
- Pin commit message explicitly deferred this audit to "the next dependency-audit campaign" (= this one, P7).

## 1. Inventory — pins vs SDK expectations

**expo-doctor (verbatim):**
```
Running 21 checks on your project...
21/21 checks passed. No issues detected!
```

**npm ls (key modules):** zero dedupe conflicts, single instance of every package. All `react-native` consumers resolve to the same `react-native@0.86.2`.

**Declared vs installed vs SDK-57.0.14 bundledNativeModules map:**

| package | declared | installed | SDK-57.0.14 map | note |
|---|---|---|---|---|
| expo | ~57.0.14 | 57.0.14 | (n/a) | pinned via exclude |
| expo-linking | ~57.0.6 | 57.0.6 | ~57.0.6 | pinned via exclude |
| expo-router | ~57.0.14 | 57.0.14 | ~57.0.14 | pinned via exclude |
| expo-asset | ~57.0.13 | 57.0.13 | ~57.0.12 | ahead of local map, doctor still passes |
| expo-audio | ~57.0.3 | **57.0.4** | ~57.0.3 | lockfile drift within tilde |
| expo-constants | ~57.0.11 | **57.0.13** | ~57.0.12 | lockfile drift +2 patches |
| expo-document-picker | ~57.0.1 | 57.0.1 | ~57.0.1 | exact |
| expo-file-system | ~57.0.5 | 57.0.5 | ~57.0.4 | ahead of map |
| expo-font / haptics / splash-screen / sqlite / symbols | ~… exact | exact | exact | aligned |
| expo-sharing | ~57.0.14 | 57.0.14 | ~57.0.13 | ahead of map |
| react / react-dom | 19.2.3 exact | 19.2.3 | 19.2.3 exact | |
| react-native | 0.86.2 exact | 0.86.2 | 0.86.2 exact | |
| reanimated / worklets | 4.5.1 / 0.10.1 | same | same (exact in map) | |
| gesture-handler / screens / safe-area-context / web | ~2.32.0 / ~4.26.0 / ~5.7.0 / ~0.21.0 | 2.32.0 / 4.26.2 / 5.7.0 / 0.21.2 | same ranges | screens drifted +2 within tilde |

Key empirical fact: doctor passes while asset/file-system/sharing/constants sit AHEAD of the local SDK map ⇒ doctor tolerates newer-than-map within major 57.

## 2. npm outdated (verbatim) + classification

```
Package                         Current   Wanted   Latest  Location                                     Depended by
@types/jest                     29.5.14  29.5.14   30.0.0  node_modules/@types/jest                     mobile
eslint                           9.39.5   9.39.5   10.9.0  node_modules/eslint                          mobile
expo                            57.0.14  57.0.15  57.0.15  node_modules/expo                            mobile
expo-linking                     57.0.6   57.0.7   57.0.7  node_modules/expo-linking                    mobile
expo-router                     57.0.14  57.0.15  57.0.15  node_modules/expo-router                     mobile
jest                             29.7.0   29.7.0   30.4.2  node_modules/jest                            mobile
react                            19.2.3   19.2.3   19.2.8  node_modules/react                           mobile
react-dom                        19.2.3   19.2.3   19.2.8  node_modules/react-dom                       mobile
react-native                     0.86.2   0.86.2   0.87.0  node_modules/react-native                    mobile
react-native-gesture-handler     2.32.0   2.32.0    3.2.1  node_modules/react-native-gesture-handler    mobile
react-native-reanimated           4.5.1    4.5.1    4.6.0  node_modules/react-native-reanimated         mobile
react-native-safe-area-context    5.7.0    5.7.0    5.9.1  node_modules/react-native-safe-area-context  mobile
react-native-screens             4.26.2   4.26.2   4.27.0  node_modules/react-native-screens            mobile
react-native-worklets            0.10.1   0.10.1   0.12.1  node_modules/react-native-worklets           mobile
typescript                        6.0.3    6.0.3    7.0.2  node_modules/typescript                      mobile
```

Classification:
- **SAFE PATCH BUMP**: only the excluded trio (Wanted=Latest, all patch releases from the same 2026-08-20 monorepo snapshot; .14 published 2026-08-17).
- **UNSAFE CHURN / HOLD**: RN 0.87.0 (SDK expects exactly 0.86.2), GH 3.2.1 (major; map ~2.32.0), reanimated 4.6.0 + worklets 0.12.1 (map pins exact), screens 4.27.0 / safe-area-context 5.9.1 (outside map), react/dom 19.2.8 (SDK exact-pins 19.2.3), ts7/eslint10/jest30/@types-jest30 tooling majors.
- **DEPRECATED**: none observed in any output.
- **UNUSED**: none removable (see §4).
- **TRANSITIVE-REQUIRED**: worklets (reanimated dep + auto babel plugin), GH/screens/safe-area/reanimated (router peers/native consumers), linking/constants/font (platform infra), asset/splash-screen/sqlite/audio/sharing (app.json config plugins).

## 3. Pin-lift evaluation (trio as a group)

Upstream changelogs were NOT quickly fetchable (releases.atom lacks entries; GitHub API tag candidates 404). Stronger evidence obtained instead — dependency metadata of the actual target versions:

- **Coupling (verified)**: `expo-router@57.0.15` peerDeps require `expo-linking ^57.0.7` and `expo-constants ^57.0.13` ⇒ selective lift impossible; trio must move together. `expo-linking@57.0.7` deps: invariant + `expo-constants ~57.0.13`.
- **Transitive deltas if lifted**: `expo-modules-core ~57.0.11→~57.0.12` (**native code**), `@expo/cli ^57.0.16→^57.0.17`, `@expo/metro-config ~57.0.8→~57.0.9`, `@expo/metro-runtime ~57.0.11→~57.0.12`, `@expo/ui ~57.0.11→~57.0.12` (router deps).
- **DECISIVE CHECK**: downloaded `expo@57.0.15` tarball (to system temp only) and diffed its `bundledNativeModules.json` against installed 57.0.14's: 30 expectations raised, and after the bump **every package we declare matches the new SDK map exactly** (asset ~.13, audio ~.4, constants ~.13, file-system ~.5, linking ~.7, router ~.15, sharing ~.14; font/haptics/document-picker/sqlite/symbols/splash unchanged; react 19.2.3 + RN 0.86.2 unchanged). Post-bump doctor alignment is verified-by-construction ⇒ expected 21/21.
- **Risk of group bump: LOW-MEDIUM.** Coordinated same-day patch releases, fully consistent graph, no peer conflicts remaining. Residual risk = expo-modules-core native patch behavior on device (covered by existing canary journeys).

## 4. Unused-dependency check (census of src/ jest/ plugins/ scripts/ + root configs; import/require/dynamic-import/jest.mock specifiers)

Directly imported: expo-router (101 + subpath imports incl. testing-library/unstable-native-tabs/ui), expo-file-system (6), expo-document-picker (5), expo-sharing (4), expo-audio (3), expo-haptics (3), **expo-symbols (2 — USED**, e.g. symbol rendering components**)**, expo-sqlite (1), react-native-safe-area-context (2), better-sqlite3 (1, node test adapter), @expo/config-plugins (2, local plugins).

Zero direct imports but NOT removable: expo (runtime platform/jest-expo preset/metro config via `expo/metro-config`), react-dom + react-native-web (web static output infra, resolution-only), expo-asset + expo-splash-screen + expo-sqlite + expo-audio + expo-sharing (app.json config-plugin usage/CNG), expo-font (SDK infra), react-native-reanimated/gesture-handler/worklets/screens (router peers + native autolinking + babel-preset-expo auto-plugin — verified zero src references via rg exit-1 sweep).

**Verdict: no declared-but-unimported removable packages.**

## 5. Native rebuild implications

Lifting the trio pins pulls native `expo-modules-core` 57.0.11→~57.0.12 ⇒ **fresh CNG prebuild regeneration + dev-client rebuild REQUIRED** before any device QA (committed `android/` tree; `with-android-ndk-pin` plugin unaffected functionally). Deferring pins = no rebuild needed now. JS-only bumps would not need a rebuild, but no such bump is proposed. Rebuild freshness guidance already exists at `scripts/qa/README.md` (011 lesson).

## 6. Recommendation table

| package | current (declared/installed) | proposed | action | risk | evidence |
|---|---|---|---|---|---|
| expo | ~57.0.14 / 57.0.14 | ~57.0.15 | LIFT PIN + bump floor | LOW-MED | §3 tarball diff; new map matches our set exactly |
| expo-linking | ~57.0.6 / 57.0.6 | ~57.0.7 | LIFT PIN + bump floor | LOW-MED | router .15 peer requires ^57.0.7 |
| expo-router | ~57.0.14 / 57.0.14 | ~57.0.15 | LIFT PIN + bump floor | LOW-MED | coupled trio; patch release |
| expo-constants | ~57.0.11 / 57.0.13 | ~57.0.13 | raise declared floor (align with installed + new map) | LOW | lockfile already resolved .13; new map expects ~.13 |
| expo-audio | ~57.0.3 / 57.0.4 | ~57.0.4 (optional) | raise declared floor | LOW | cosmetic alignment with new map |
| expo-asset/-file-system/-sharing | as-is | NO CHANGE | keep | — | already match post-bump map exactly |
| sqlite/symbols/haptics/font/splash/document-picker | as-is | NO CHANGE | keep | — | unchanged in new map |
| react / react-dom | 19.2.3 | NO CHANGE | hold (SDK exact pin) | — | map: 19.2.3 exact |
| react-native | 0.86.2 | NO CHANGE | hold | — | SDK-locked; 0.87.0 is next-SDK territory |
| reanimated/worklets/screens/safe-area/GH | as-is | NO CHANGE | hold until SDK 58 wave | HIGH if bumped now | latest outside map; GH3 is a major |
| jest/jest-expo/@testing-library | 29.7/~57.0.4/^14.0.1 | NO CHANGE | hold; jest-expo matches map | — | in-range clean |
| typescript/eslint/@types/jest | 6.0.3/9.39.5/29.5.14 | DEFER majors (ts7, eslint10, @types/jest30) | separate tooling wave | MED | majors; zero audit value now |
| better-sqlite3 + @types | ^13.0.3/^9.6.0 | NO CHANGE | dev/test infra | — | works |

### Exact manifest edit requested (parent applies)
1. In `apps/mobile/package.json`: set `expo: "~57.0.15"`, `expo-linking: "~57.0.7"`, `expo-router: "~57.0.15"`, `expo-constants: "~57.0.13"` (optionally `expo-audio: "~57.0.4"`); DELETE the whole `"expo": { "install": { "exclude": ... } }` block.
2. `npm install` in apps/mobile (lockfile refresh by parent only).
3. Then parent-owned validation gate: `npx expo-doctor` (expect 21/21), Jest suite, CNG prebuild regen + dev-client rebuild, boot smoke + representative canary journey.

## 7. Verdict on lifting pins

Technically GREEN to lift now: all evidence aligns, coupling satisfied, doctor pass verified-by-construction, upstream only 3 days newer than validated state. Operationally recommend applying at the next wave boundary rather than mid-games-wave: mandatory native rebuild invalidates concurrent sibling device state (same rationale that motivated 011's deferral), and KNOWN_ISSUES records the one-Metro/no-src-edit-during-journeys discipline. Final timing = parent call.

## NEEDS_PARENT
1. Lift timing: now vs next wave boundary (recommend boundary; §7).
2. Optional cosmetic floors (expo-audio ~.4): approve or skip.
3. Changelogs for the three patches were not fetchable programmatically — if release-notes review is required before lift, needs manual GitHub check.

## Validation
- All commands read-only (doctor/npm ls/outdated/view/pack-to-temp/tar-extract-to-temp/rg census/node metadata reads).
- Temp artifacts used: `%TEMP%\w15-import-census.mjs`, `%TEMP%\w15\*` (removed after use).
- `git status --porcelain` after my work shows ONLY `.agent/_tasks/campaign012/W15.md` added by me; all other dirty paths are pre-existing sibling games edits (`apps/mobile/src/games/**`).
- Untracked `apps/mobile/w09-verify.mjs` appeared mid-session (absent at my session-start baseline) — sibling W09's artifact, NOT created by W15; flagged so parent attributes it correctly.
