# W15 — Dependency Audit

**Status:** PENDING

## Mission
Controlled audit of the dependency surface after Campaign 011's intentional patch pins.
Report-first: the parent applies any lockfile/manifest change.

## Owned paths (write)
- `.agent/_tasks/campaign012/W15.md` (your findings) — that is all you may write
- Optionally a throwaway audit script under `C:\Users\palac\AppData\Local\Temp\opencode`

## Read-only dependencies
- `apps/mobile/package.json`, root `package.json`, lockfiles, `app.json` install.exclude,
  `node_modules` metadata (`npm ls <pkg>`), `npx expo-doctor` output

## Forbidden paths
package.json / package-lock.json / app.json edits (parent applies approved changes),
all source, scripts, docs.

## Implementation targets
1. Inventory current pins vs SDK-expected versions: expo, expo-router, expo-linking,
   native modules (sqlite, audio, haptics, asset, file-system, sharing, etc.), react-native,
   test tooling
2. Classify each drift: safe patch bump / unsafe churn / deprecated / unused /
   transitive-required
3. Explicitly evaluate lifting the 011 `expo.install.exclude` pins (expo ~57.0.14-era):
   what changed upstream? risk of bumping together as a group?
4. Unused-dependency check (imports vs declared)
5. Produce a concrete recommendation table: package | current | proposed | action | risk |
   evidence. NO changes without parent approval.

## Validation expectations
- Commands run read-only; record outputs in your packet
- No working-tree modifications outside your packet file

## Shared changes requested from parent
_(worker fills — the exact manifest/lockfile edits you recommend)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
