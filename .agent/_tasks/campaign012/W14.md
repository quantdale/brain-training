# W14 — Android Build / Config / Permissions

**Status:** COMPLETED

## Mission
Make the Android build surface cleaner and more deterministic. Campaign 011 codified CNG
config into committed plugins; audit and complete that story.

## Owned paths (write)
- `apps/mobile/app.json` (expo config) — parent-converged if conflicts appear
- `apps/mobile/plugins/**` (config plugins + their tests)
- `scripts/android/**` build/config helper scripts
- `apps/mobile/eas.json` / build profiles if present

## Read-only dependencies
- Generated `android/` output (never commit it), expo prebuild behavior
- `package.json` (read-only; dependency changes are W15/parent)

## Forbidden paths
`apps/mobile/android/**` (generated; never edit/commit), all src code, registry,
lockfiles, `.agent/`.

## Implementation targets
1. Audit permissions: manifest permissions vs actual feature use; blockedPermissions current
2. Audit backup rules + data extraction rules XMLs (011 plugins) for completeness/correctness
3. NDK pin plugin: verify still correct for the host toolchain story documented in 011
4. Package identity/version metadata consistency (app.json vs plugins)
5. Build profiles: debug/release differences intentional and documented
6. Prove determinism: run `npx expo prebuild --platform android --no-install` into a
   TEMPORARY directory (never the repo's android/ unless already gitignored workflow),
   diff key outputs, record evidence; clean up temp output
7. Document any drift found + fixes in Completion summary

## Validation expectations
- `node --check` on changed scripts; plugin unit tests green
- Prebuild reconstruction evidence recorded (PASS with artifacts or NOT VALIDATED + why)
- No generated android/ files staged

## Shared changes requested from parent
_(worker fills)_

## Completion summary

Android build determinism: permissions audited (INTERNET/VIBRATE/MODIFY_AUDIO_SETTINGS/legacy-storage intentional; SYSTEM_ALERT_WINDOW only in debug manifest); RECORD_AUDIO added to blockedPermissions as defense-in-depth; backup/data-extraction rules verified complete vs B7 policy (generated bytes === plugin exports); NDK pin plugin refactored to pure applyNdkPin(props) + 5-test suite (plugin suites 9/9); identity consistency verified; no eas.json intentionally (local builds). PREBUILD DETERMINISM PROVEN: expo prebuild --platform android --no-install into two separate temp dirs, EXPO_OFFLINE=1 - identical file lists, 0 differing contents, key hashes recorded (manifest/data-extraction/backup-rules/gradle.properties). Stale local gitignored android/ flagged: regenerate before next device QA.

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
