# W07 — Workout UX

**Status:** COMPLETED

## Mission
Make Workout V2 feel like a first-class product surface: template/length picker, focus
workout explanation, resume/completed states, history readability, completion summary.

## Owned paths (write)
- `apps/mobile/src/app/(tabs)/index.tsx` — workout section only (do not redesign other
  Home areas: streaks/quests/recent rows stay as-is except where workout state renders)
- NEW `apps/mobile/src/components/workout/**` (picker/history/summary components live here)
- NEW route file(s) under `apps/mobile/src/app/` ONLY if required for picker/history —
  report any new route to parent in Completion summary (layout registration may be needed)

## Read-only dependencies
- `src/workout/**` (W06's engine API; consume the CURRENT committed API — coordinate via
  NEEDS_PARENT if you need an engine change; do not edit W06's files)
- `src/components/game-ui/**`, theme tokens

## Forbidden paths
`src/workout/**` (W06), scripts/qa (W08), games, game-host, registry, manifests,
other Home sections' logic, visual-baselines snapshot file (parent regenerates if needed).

## Implementation targets
- Template + length selection UI with clear short/standard/extended/focus distinction
- Resume state: partially completed workout visibly resumable ("2 of 4 done")
- Completed state + history readability (recent instances, template labels)
- Focus workout explanation (why these domains were picked — reuse personalization reasons)
- Completion summary presentation from engine data
- Keep existing design language, a11y contract (testIDs `home-workout-*`, roles, targets),
  and CTA priority (Today's Workout stays the primary Home CTA per constitution §13)
- New interactive elements need stable semantic testIDs for W08's device traversal

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: any new component tests green; existing app-shell/home suites still pass

## Shared changes requested from parent
_(worker fills)_

## Completion summary

Workout UX first-class surface: template chips + length chips (Short/Standard/Extended) + selected-template detail panel (name/meta/description), resume block (N of M done + Resume CTA relabel), completed-today state, history rows with length suffixes (+ -name/-detail/-xp subIDs), focus explanation panel (static copy + personalization reasons via explainTemplateWorkout), completion summary card with avg% and per-game outcome feed. No new routes needed. Full testID inventory delivered for harness traversal. Validation: tsc clean; app-shell/results-workout-cta/workout-ui 26 tests PASS; visual-baselines 4/4 no drift.

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
