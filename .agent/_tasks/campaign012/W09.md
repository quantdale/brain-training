# W09 — Math Content Quality

**Status:** PENDING

## Mission
Resolve the equation-builder dead easy templates debt and audit math content integrity
across the four/five math games.

## Known debt (010 finding)
`math-equation-builder` easy level: eight pre-existing 3-number templates requiring ×
are unreachable because easy's +/− operator mix always fails their solvability filter:
[10,3,4]→26, [8,7,3]→53, [6,5,4]→54, [9,4,2]→38, [7,6,3]→45, [10,5,2]→52, [8,6,4]→44,
[13,2,5]→31. Options: re-tier / rewrite / replace / remove — no unreachable content.

## Owned paths (write)
- `apps/mobile/src/games/math-*/generator.ts`, content/template banks, `difficulty.ts`
  (incl. `math-equation-builder/generator.ts` + `difficulty.ts`)
- Math game generator/difficulty tests

## Read-only dependencies
- Math game reducers/scoring/screens (read-only; mechanics and scoring stay untouched
  unless a strong documented reason — then NEEDS_PARENT + regression proof)

## Forbidden paths
Screens/components/session files of any game (W01–W04 own those), non-math games (W10),
registry, versions.ts bumps are allowed ONLY for your owned games per provenance rules,
manifests/scripts/docs/.agent/.

## Implementation targets
1. Fix the 8 dead templates intelligently (prefer re-tier to hard/rewrite; prove reachability)
2. Solvability: every template reachable at its tier must generate valid puzzles across
   seeds (property-style test sweep)
3. Difficulty tier consistency across math games (easy actually easy; expert meaningful)
4. Duplicate/near-duplicate content pruning; dead/unreachable branches removal
5. Expert content balance: no degenerate/impossible expert items
6. Version discipline: bump `contentVersion`/`gameVersion` where content semantics change;
   keep historical-result reinterpretation impossible

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: math generators/difficulty suites green incl. new reachability sweeps

## Shared changes requested from parent
_(worker fills)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
