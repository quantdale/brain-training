# W06 — Workout Engine / Template Depth

**Status:** PENDING

## Mission
Finish the broader Workout V2 product semantics: short/standard/extended lengths,
domain-focus templates, rotation, resume, completion, history, metadata, reconciliation.
Make the engine a first-class surface and keep selection deterministic.

## Owned paths (write)
- `apps/mobile/src/workout/**`
- workout repository code if gaps found (`src/db/workouts.ts` or equivalent) — coordinate
  via NEEDS_PARENT before any schema/migration change (schema is parent-owned; NO new migrations)

## Read-only dependencies
- `src/db/**`, `src/personalization/**`, game registry/catalog
- Campaign 011 device evidence: default daily journey PASS (`qa-artifacts/20260822-113955-autobot-workout`)

## Forbidden paths
Home UI (`src/app/**`, home components — W07), QA scripts (W08), manifests, registry,
db migrations/schema files, `.agent/`.

## Implementation targets
- Template semantics: short / standard / extended / domain-focus — verify each length's
  game count, distinctness constraints, domain targeting, and rotation behavior
- Resume/reconcile: partial progress + kill/relaunch + date rollover edge cases
- Completion summary data: per-game outcomes + aggregate readiness for UI (W07 consumes)
- History: readable instance history incl. template/length metadata round-trip
- Determinism: same profile+date+seed → same selection; reroll changes exactly the tail
  (011 regression) stays fixed
- Test catalog sizes beyond 42 conceptually (e.g. synthetic 60-game registries in tests)
- Do NOT alter proven advance/persistence mechanics that passed the 011 device journey
  without a strong reason; if you must, add regression tests proving the journey still holds

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: `src/workout/__tests__/**` green (+ any touched db repo suites)

## Shared changes requested from parent
_(worker fills)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
