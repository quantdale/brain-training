# W04 — GameHost Migration Batch D (special lifecycle)

**Status:** COMPLETED

## Mission
Migrate the final 6 high-complexity / special-lifecycle legacy games onto
`@/components/game-host`: `memory`, `memory-pair-recall`, `memory-prospective-cue`,
`math-equation-builder`, `spatial-grid-nav`, `flexibility-color-stroop`.

Same contract, method, and exemplars as W01 (read that packet).

NOTE: within `math-equation-builder`, W09 owns generator/content/difficulty files —
you own only screen/components/tests. Do not touch its generator.

## Owned paths (write)
- `apps/mobile/src/games/memory/**`
- `apps/mobile/src/games/memory-pair-recall/**`
- `apps/mobile/src/games/memory-prospective-cue/**`
- `apps/mobile/src/games/math-equation-builder/**` EXCEPT generator.ts/content/difficulty.ts (W09)
- `apps/mobile/src/games/spatial-grid-nav/**`
- `apps/mobile/src/games/flexibility-color-stroop/**`

## Read-only dependencies
`src/components/game-host/**`, `src/components/game-ui/**`, `src/sdk/**`;
exemplars `.agent/_tasks/campaign010/W17.md`/`W18.md` + `games/math-fast-math/screen.tsx`.

## Forbidden paths
Everything else (other games incl. equation-builder generator/difficulty, game-host itself,
scanner, registry, manifests, scripts, docs).

## Implementation targets
W01 targets apply. Special care:
- `spatial-grid-nav`: the 011 Critical a11y fix (decorative option boards unmount while
  paused) MUST survive migration; Resume/Quit must stay reachable in the a11y tree.
  Keep force-win path automation-friendly.
- `flexibility-color-stroop`: keep all 009 stroop defect fixes (neutral trials, RT handling,
  flip-cue phase); multi-phase `canPause()` precision required.
- `memory-prospective-cue`: keep 011 response bleed-through fix.
- `math-equation-builder`: preserve 3-step tutorial demo flow and undo fix.
- If any game is genuinely a poor GameHost fit, migrate what is clean and REPORT the rest
  with reasons. Correctness beats numeric completion.

## Validation expectations
`tsc --noEmit` clean; owned suites + catalog-contracts suite green.

## Shared changes requested from parent
_(worker fills)_

## Completion summary

Final 6 special-lifecycle games migrated (memory, memory-pair-recall, memory-prospective-cue, math-equation-builder screen-only, spatial-grid-nav, flexibility-color-stroop). All critical fixes survived migration: grid-nav decorative boards unmount while paused (011 a11y), stroop 009 defect cluster, prospective-cue bleed-through fix, equation-builder tutorial demo + undo fix. Zero test modifications required. Validation: tsc clean twice; 73 suites/832 tests PASS. Catalog roster of non-migrated screens now 0.

## Remaining debt

See summary; residual items tracked in .agent/KNOWN_ISSUES.md and .agent/BACKLOG.md.
