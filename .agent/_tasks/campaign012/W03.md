# W03 — GameHost Migration Batch C (complex board / spatial / content-driven)

**Status:** PENDING

## Mission
Migrate 6 complex legacy games onto `@/components/game-host`:
`language-sentence-builder`, `language-word-match`, `logic-deduction-table`, `logic-rule-grid`,
`math-number-line-estimation`, `spatial-mental-rotation`.

Same contract, method, and exemplars as W01 (read that packet).

## Owned paths (write)
- `apps/mobile/src/games/language-sentence-builder/**`
- `apps/mobile/src/games/language-word-match/**`
- `apps/mobile/src/games/logic-deduction-table/**`
- `apps/mobile/src/games/logic-rule-grid/**`
- `apps/mobile/src/games/math-number-line-estimation/**`
- `apps/mobile/src/games/spatial-mental-rotation/**`

## Read-only dependencies
`src/components/game-host/**`, `src/components/game-ui/**`, `src/sdk/**`;
exemplars `.agent/_tasks/campaign010/W17.md`/`W18.md` + `games/math-fast-math/screen.tsx`.

## Forbidden paths
Everything else (other games, game-host itself, scanner, registry, manifests, scripts, docs).

## Implementation targets
W01 targets apply. Board/spatial/content care:
- decorative boards must unmount while paused (011 grid-nav-class a11y rule) if the game
  renders answer-revealing boards; verify with the pause overlay mounted
- `spatial-mental-rotation`: preserve any Skia/canvas lifecycle and force-timeout QA extra
- `logic-deduction-table`: keep the exhaustive uniqueness prover untouched (009 Critical fix)
- `language-word-match`: keep content-validation pipeline untouched

If a game's custom phase machine makes GameHost a worse fit than local composition,
migrate only what reduces complexity and report what you deliberately left local.

## Validation expectations
`tsc --noEmit` clean; owned suites + catalog-contracts suite green.

## Shared changes requested from parent
_(worker fills)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
