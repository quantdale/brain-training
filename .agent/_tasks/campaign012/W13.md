# W13 — Scale / Query Maturation

**Status:** DONE (evidence-negative: probes + analysis landed; no production change justified)

## Mission
Evidence-driven performance maturation on measured expensive surfaces. Do NOT replace
simple code with complex infrastructure without evidence.

## Owned paths (write)
- `apps/mobile/src/progression/**` (sync scan internals)
- Query/aggregate internals in `src/db/**` query modules, `src/analytics/**` projections,
  backup serialization hot paths (`src/data-portability/**`)
- Perf probes under `scripts/perf/**` + baseline notes

## Read-only dependencies
- Callers of changed functions (screens/repositories) — behavior-preserving refactors only

## Forbidden paths
Schema migrations / `schema.ts` version bumps (parent-owned), UI screens, workout engine
(W06), games, manifests. Any API-signature change visible to screens → NEEDS_PARENT.

## Known starting points
- `SYNC_SESSION_SCAN_LIMIT = 5000` in `src/progression/sync.ts`: investigate whether
  achievements/quests should move toward incremental aggregates/projections/indexed
  queries/cached summaries. Choose the SIMPLEST evidence-backed improvement.
- loadProgressSnapshot measured 102.7 ms @20k (fast path) — acceptable; only improve if a
  probe shows a regression risk or cheap win.

## Implementation targets
1. Measure first: extend/run perf probes for achievement/quest scans at realistic+stress sizes
2. Implement the minimal structural fix the numbers justify (e.g., bounded incremental state)
3. Preserve correctness: differential equivalence tests where an aggregate replaces a scan
4. Document before/after numbers in scripts/perf/baselines + your Completion summary

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: progression/analytics/data-portability suites green incl. equivalence tests
- Probe runs recorded with honest numbers (NOT VALIDATED if not run)

## Shared changes requested from parent

1. **NEEDS_PARENT (quests engine, `src/quests/evaluate.ts`):** quest sync cost at large
   histories is in-JS evaluation, not DB reads (~78 ms @ the 5000-sample cap on desktop
   node; realistic 100-session histories cost ~2 ms). A real fix = compute day/week keys
   once per sample inside `evaluateQuests` (or numeric period indices instead of string
   compares). The caller-side single-pass partition variant was implemented and REJECTED:
   measured ~15% slower under babel/jest (`perf-quest-eval-ab-2026-08-22.json`).
2. **NEEDS_PARENT (schema):** `buildAchievementSnapshot` ≈ 42–62 ms @20k from ~11
   index-unable full-scan aggregates over `game_sessions` (≤3 ms @1k). Fix requires
   indexed/materialized aggregate columns → SCHEMA_VERSION bump (parent-owned).

## Completion summary

- Measured achievement/quest sync at 100/1k/5k/20k sessions (new opt-in
  `perf-sync-scan-probe.test.ts`, min-of-3 methodology; captured via extended
  `run-probes.mjs`) → `scripts/perf/baselines/sync-scan-baselines.md` + 3 JSON artifacts.
- Finding: DB reads already cheap (≤12 ms @20k); cost is in-JS quest evaluation near the
  `SYNC_SESSION_SCAN_LIMIT` cap and SQL aggregate scans for the achievement snapshot.
- Explored single-pass period-partition refactor of `sync.ts`: differential-equivalence-
  tested, then reverted after order-robust in-process A/B showed a consistent regression
  under the project toolchain (details + repro spec kept in `perf-quest-eval-ab.test.ts`).
- No behavior change shipped; no equivalence test required (no aggregate replaced a scan).
- Validation: `npx tsc --noEmit` CLEAN; targeted jest green — 23 suites / 289 tests passed
  (progression, analytics, data-portability, db scale, perf query-pattern guards; probe
  specs verified skipped in normal CI). `node scripts/perf/run-probes.mjs` run end-to-end,
  both baselines written.

## Remaining debt

- Quest evaluation O(definitions × samples) with per-sample Date/string churn — needs the
  parent-owned engine fix above before any further caller-side work is worthwhile.
- Achievement snapshot full-scan aggregates — schema-owned fix as above.
- Probe absolute numbers are desktop-node and machine-relative; re-baseline after any fix
  lands (same-machine comparisons only).
