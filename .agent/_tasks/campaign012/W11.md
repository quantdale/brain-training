# W11 — Product UX Polish (Games / Progress / Rewards / Results / Game Detail)

**Status:** PENDING

## Mission
Release-candidate polish across existing workflows. No uncontrolled redesign; keep the
existing design language.

## Owned paths (write)
- `apps/mobile/src/app/(tabs)/games.tsx`
- `apps/mobile/src/app/(tabs)/progress.tsx`
- `apps/mobile/src/app/progress-activity.tsx`, `progress-detail.tsx`, `progress-domain.tsx`,
  `progress-game.tsx`
- `apps/mobile/src/app/rewards.tsx`
- `apps/mobile/src/app/results.tsx` (polish only — the workout advance flow is proven;
  do not restructure its mechanics)
- `apps/mobile/src/app/game-detail/[id].tsx`
- Shared components used ONLY by these screens (new files under `src/components/…` if needed)

## Read-only dependencies
- Home (W07 owns workout section; you own nothing there), Profile/Settings/Data Mgmt (W12)
- `src/analytics/**`, `src/db/**`, `src/economy/**`, `src/components/game-ui/**` — consume only

## Forbidden paths
Home tab, profile/settings/data-management, games, workout engine, scripts, registry,
manifests, visual-baselines snapshot (parent regenerates), `.agent/`.

## Implementation targets
- State hierarchy: loading/empty/error/content consistently ordered and styled
- Empty states with guidance; error states with recovery CTAs
- CTA priority + drill-down affordances (Progress → detail flows)
- Section ordering + spacing consistency using theme tokens
- Status clarity (e.g., PB/new-record markers, rating movement explanations)
- Keep all testIDs stable; add new ones only for genuinely new controls

## Validation expectations
- `npx tsc --noEmit` clean from apps/mobile
- Targeted Jest: touched screen suites green (progress-insights, results-workout-cta,
  progress-detail suites must stay green)

## Shared changes requested from parent
_(worker fills)_

## Completion summary
_(worker fills)_

## Remaining debt
_(worker fills)_
