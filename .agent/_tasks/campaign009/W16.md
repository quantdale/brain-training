# Worker 16 — Cross-platform / iOS static compatibility + architecture audit

## Owned write surfaces (reports only)
- `docs/audits/campaign009-xplat-audit.md` (create)
- `docs/audits/campaign009-architecture-debt.md` (create)

Everything else: READ-ONLY. Static compatibility fixes are REPORTED with exact
file/line/proposed-diff — the parent applies or routes them.

## Mission
1. Audit the whole `apps/mobile/src` tree for: Android-only assumptions;
   `Platform.OS` misuse; filesystem path assumptions (Windows-host dev,
   Android/iOS runtime); navigation compatibility; safe-area handling;
   dimension/window assumptions; touch semantics differences; native module
   usage vs Expo SDK 57 compatibility; iOS compile-risk patterns (APIs with no
   iOS implementation, unsupported props).
2. Windows cannot run Xcode: iOS build/runtime is NOT VALIDATED by definition.
   Your audit is static. Classify findings: (a) fix-now trivial static risks,
   (b) watch-list, (c) needs real macOS validation.
3. Architecture debt that should become the NEXT campaign rather than scope
   creep now: identify 3-6 concrete candidates with rationale, effort estimate,
   and risk. Examples to evaluate: shared result-screen consolidation, catalog
   content-pack architecture, sync-readiness seams, QA harness scaling,
   performance instrumentation, a11y hardening program.

## Validation
- Your two report files exist, are precise (file:line references), and tsc/lint
  remain untouched by you (you should change no code).
