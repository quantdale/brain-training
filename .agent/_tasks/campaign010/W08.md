# W08 — Progress / Analytics V2

## Mission

Materially deepen Progress analytics on top of Campaign 009's work. All metrics derived from stored data; no cognitive-improvement or medical-efficacy claims anywhere.

Implement (new pure modules under `src/analytics/` encouraged):
- domain trend summaries + game trend summaries
- session volume views, accuracy trends, reaction trends
- difficulty progression views
- personal-best history
- training balance enhancements
- activity calendar enhancements
- recent-vs-lifetime refinements, rolling windows
- category comparison
- workout completion analytics (consume workout APIs read-only)
- streak correlation presentation (presentation of co-occurrence, not causation)

Surface them in the progress screens you own (`app/(tabs)/progress.tsx`, `app/progress-detail.tsx`, `app/progress-domain.tsx`, `app/progress-game.tsx`, `app/progress-activity.tsx`) and `components/progress-charts.tsx`.

HARD constraint: `src/analytics/queries.ts` is owned by W09 (performance rewrite). Do NOT edit it. Consume its existing public API; if you need a new query/projection, add a `NEEDS_PARENT:` block with the exact desired signature and continue with what exists.

## Owned paths
- `apps/mobile/src/analytics/**` EXCEPT `analytics/queries.ts`
- `apps/mobile/src/app/(tabs)/progress.tsx`
- `apps/mobile/src/app/progress-detail.tsx`
- `apps/mobile/src/app/progress-domain.tsx`
- `apps/mobile/src/app/progress-game.tsx`
- `apps/mobile/src/app/progress-activity.tsx`
- `apps/mobile/src/components/progress-charts.tsx`
- This packet file (Status/summary sections only)

## Read-only dependencies
- `analytics/queries.ts` (API as-is), `db/**` read patterns, workout types, theme tokens

## Forbidden paths
`analytics/queries.ts`, `db/**`, other app screens, `components/a11y*`/`game-ui/**` (W14), registry, package.json.

## Implementation backlog
- [ ] Trend summary modules (domain/game/volume/accuracy/reaction/difficulty)
- [ ] Personal-best history + rolling windows + category comparison
- [ ] Workout completion analytics
- [ ] Screen surfacing + charts
- [ ] New-player guards so empty states stay sane

## Shared changes requested from parent
(none yet)

## Status
IN PROGRESS

## Implementation summary
(filled at end)

## NOT VALIDATED gaps
(filled at end)
