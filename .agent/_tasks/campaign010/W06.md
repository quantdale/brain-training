# W06 — Workout Engine V2

## Mission

Expand the Workout system beyond basic four-game daily selection, keeping current default Today's Workout behavior intact unless the architecture clearly requires migration.

Implement production architecture for:
- workout templates (named structures beyond the default daily mix)
- focus workouts (domain-targeted)
- short / standard / extended workout lengths
- resume/reconciliation hardening across template types
- workout history (queryable record of past workouts + completion state)
- workout metadata (template id, length, focus, generation inputs — versioned)
- completion summaries (per-workout aggregate for results/UI)
- personalization reasons surfaced through the existing personalize seam (read-only consumer of W07's outputs — do NOT edit `workout/personalize.ts`)
- rotation strategy across days/templates (deterministic, injectable clock)

You own `src/db/workout.ts` — the single db file exception for this packet. Preserve migration compatibility; no schema changes unless genuinely required (then document precisely).

HARD constraints:
- `src/workout/personalize.ts` is owned by W07. Consume its current public API as-is; NEEDS_PARENT if it changes underneath you.
- UI surfacing (home tab screen) belongs to W13 — you deliver engine APIs + types they can consume. NEEDS_PARENT for screen changes.
- No monetization restrictions.

## Owned paths
- `apps/mobile/src/workout/**` EXCEPT `workout/personalize.ts`
- `apps/mobile/src/db/workout.ts`
- This packet file (Status/summary sections only)

## Read-only dependencies
- `workout/personalize.ts` (API as-is), `db/schema.ts`, registry/catalog metadata, game result contracts

## Forbidden paths
`workout/personalize.ts`, all other `db/*` files, screens, registry, package.json.

## Implementation backlog
- [ ] Template model + named templates (focus/domain-targeted)
- [ ] Length variants (short/standard/extended)
- [ ] History storage + queries (db/workout.ts)
- [ ] Resume/reconciliation across templates
- [ ] Completion summaries + metadata versioning
- [ ] Rotation strategy (deterministic)

## Shared changes requested from parent
(none yet)

## Status
IN PROGRESS

## Implementation summary
(filled at end)

## NOT VALIDATED gaps
(filled at end)
