# W10 — Backup / Import / Storage V2

## Mission

Implement the Campaign 009 portability debt items: single-pass serialization + production transport wiring (architecture-debt D2).

Serialization performance:
- Single-pass canonical serialization (measured debt: export canonicalizes the envelope twice, ~2.4s frozen JS @5k sessions).
- Reduced duplicate JSON processing across serialize/preview/import paths.
- Large-backup memory-conscious paths (streaming/chunked where practical without changing the format contract).

Transport production wiring (`src/data-portability/transport.ts` currently ships only `createMemoryTransport()`):
- `FileBackupTransport` via expo-file-system under `documentDirectory` (deps ALREADY installed by parent: `expo-file-system`, `expo-sharing`, `expo-document-picker` — verify versions in package.json before importing).
- Share-sheet handoff seam via expo-sharing (export), restore source via expo-document-picker (import).
- Clean transport abstraction; import-source abstraction.
- Versioned metadata improvements + better backup manifest.

Maintain absolutely:
- offline-first, checksums, atomic restore, merge/replace behavior, rollback safety, local singleton profile normalization (009 fix), typed early rejection, DoS gate.

HARD constraints:
- No cloud sync. No new dependencies (already installed for you).
- `app/data-management.tsx` is owned by W13 — keep the public API you expose backward-compatible with its current usage; breaking changes → NEEDS_PARENT.
- Expo SDK 57 docs before using the three packages (per apps/mobile/AGENTS.md).

## Owned paths
- `apps/mobile/src/data-portability/**`
- This packet file (Status/summary sections only)

## Read-only dependencies
- `db/**` read/export APIs, `app/data-management.tsx` (consumer), jest setup mocks (to know what exists)

## Forbidden paths
`db/**`, screens, registry, package.json, app.json.

## Implementation backlog
- [ ] Single-pass canonical serializer
- [ ] Duplicate-JSON-processing audit + fixes
- [ ] FileBackupTransport + share/picker seams
- [ ] Manifest/metadata versioning
- [ ] Memory-conscious large-backup path

## Shared changes requested from parent
(none yet)

## Status
IN PROGRESS

## Implementation summary
(filled at end)

## NOT VALIDATED gaps
(filled at end)
