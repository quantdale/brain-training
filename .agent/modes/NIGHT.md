# Night / Aggressive Mode

Purpose: use more workstation resources when the owner intentionally allows it.

- May use more CPU/RAM and heavier validation.
- May increase suitable worker concurrency, respecting safe file ownership.
- Still default to one Android emulator unless evidence later supports more.
- No host mouse/keyboard/focus automation.
- Does not automatically become a full hardening campaign.

The user explicitly selects Night mode; never infer it from local time.
