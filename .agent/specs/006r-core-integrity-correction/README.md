# Campaign 006R Deep-Audit Working Papers

**Status:** SUPPORTING MATERIAL — execution authority moved to OpenSpec.

The detailed audit that produced Campaign 006R is retained in this directory for provenance and additional implementation context. Autonomous execution MUST use:

`openspec/changes/006r-core-integrity-correction/`

Authority order for this campaign:

1. `docs/PROJECT_CONSTITUTION.md`
2. OpenSpec `proposal.md`
3. OpenSpec `design.md`
4. OpenSpec `specs/**/spec.md`
5. OpenSpec `tasks.md`
6. this `.agent/specs/**` audit material

If an old numbered audit spec conflicts with the active OpenSpec change, follow OpenSpec and update the working paper later if useful. Do not execute two competing plans in parallel.

The original numbered files `001`–`013` remain useful for root-cause notes, affected-file hints, and audit provenance.